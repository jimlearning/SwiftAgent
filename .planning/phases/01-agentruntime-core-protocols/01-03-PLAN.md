---
phase: 01-agentruntime-core-protocols
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/AgentProfile.swift
  - Sources/SwiftAgentCore/AgentRuntime/Graph/AgentGraph.swift
  - Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift
autonomous: true
requirements:
  - TOOL-01
  - RUNTIME-06
  - GRAPH-01
  - RUNTIME-01

must_haves:
  truths:
    - "Simplified Tool protocol (named AgentTool) exists with ~6 members coexisting alongside existing 43-member Tool protocol"
    - "AgentProfile struct exists bundling agent identity: name, instructions, tools, model, permissionMode, memoryScope"
    - "AgentGraph protocol + AgentNode concept exist as type-slots (not implemented) with NodeInput, NodeOutput, NodeCondition types"
    - "AgentRuntime actor protocol exists with property slots for all 8 subsystems: modelProvider, memoryStore, permissionEngine, toolEngine, contextManager, profileManager, graphEngine, hookSystem"
  artifacts:
    - path: "Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift"
      provides: "Simplified Tool protocol (~6 members) with primary associated type"
      contains: "protocol AgentTool"
      exports: ["AgentTool"]
    - path: "Sources/SwiftAgentCore/AgentRuntime/AgentProfile.swift"
      provides: "Agent identity bundle"
      contains: "struct AgentProfile"
      exports: ["AgentProfile"]
    - path: "Sources/SwiftAgentCore/AgentRuntime/Graph/AgentGraph.swift"
      provides: "AgentGraph + AgentNode type-slots (design only, not implemented)"
      contains: "protocol AgentGraph"
      exports: ["AgentGraph", "AgentNode", "NodeInput", "NodeOutput", "NodeIOType", "NodeCondition"]
    - path: "Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift"
      provides: "Central actor protocol with subsystem property slots"
      contains: "protocol AgentRuntime"
      exports: ["AgentRuntime", "ToolEngine", "ContextManager", "ProfileManager", "HookSystem"]
  key_links:
    - from: "AgentRuntime.swift (modelProvider)"
      to: "LanguageModel.swift (LanguageModel protocol from plan 01-02)"
      via: "var modelProvider: any LanguageModel"
    - from: "AgentRuntime.swift (memoryStore)"
      to: "MemoryStore.swift (MemoryStore protocol from plan 01-02)"
      via: "var memoryStore: any MemoryStore"
    - from: "AgentRuntime.swift (permissionEngine)"
      to: "PermissionEngine.swift (PermissionEngine protocol from plan 01-01)"
      via: "var permissionEngine: any PermissionEngine"
    - from: "AgentProfile.swift (tools)"
      to: "AgentTool.swift (AgentTool protocol)"
      via: "var tools: [any AgentTool]"
    - from: "AgentProfile.swift (permissionMode)"
      to: "PermissionEngine.swift (AgentPermission enum from plan 01-01)"
      via: "var permissionMode: AgentPermission"
---

<objective>
Define the top-level Agent types that form the public API surface — the simplified Tool protocol, the AgentProfile identity bundle, the AgentGraph placeholder type-slots, and the central AgentRuntime actor protocol. These types depend on the foundation and provider types from plans 01-01 and 01-02. Together, the three plans define all 16 new types required by Phase 1.

Purpose: Complete the Phase 1 type system. After this plan, the AgentRuntime blueprint is fully defined — all protocols, structs, enums, and type-slots exist. Phase 2 can wire them into a working runtime loop.

Output: 4 new Swift files (1 existing file edited to add placeholder protocols). All types are additive. Zero existing code modified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-agentruntime-core-protocols/01-RESEARCH.md

Key reference sections from RESEARCH.md:
- Code Examples > Simplified Tool Protocol (lines 677-705)
- Code Examples > AgentProfile + MemoryScope (lines 571-623)
- Code Examples > AgentGraph Placeholder (lines 921-985)
- Architecture Patterns > Pattern 1 (Actor Protocol with Subsystem Slots, lines 188-237)
- Architecture Patterns > Pattern 4 (Protocol with Primary Associated Type, lines 317-351)
- Pitfalls > Pitfall 2 (Primary Associated Type Shadowing, lines 402-410)
- Pitfalls > Pitfall 1 (Actor Protocol Property Access, lines 392-400)
- Anti-Patterns (lines 365-370): No @unchecked Sendable, no nonisolated(unsafe), no public class types

@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md (Pattern 4: Profile Concept, lines 331-349)

Interface contracts from completed plans:

From plan 01-01:
- AgentPermission enum (13 cases) in PermissionEngine.swift — consumed by AgentProfile.permissionMode
- PermissionEngine protocol in PermissionEngine.swift — consumed by AgentRuntime.permissionEngine
- Transcript struct in Transcript.swift — consumed by AgentRuntime (indirectly)
- MemoryScope enum in Transcript.swift — consumed by AgentProfile.memoryScope
- ToolOutput enum in ToolOutput.swift — consumed by AgentTool.call() return type
- GenerationChannel protocol — not directly consumed by this plan's types

From plan 01-02:
- LanguageModel protocol in LanguageModel.swift — consumed by AgentRuntime.modelProvider and AgentProfile.model
- LanguageModelCapabilities struct in LanguageModel.swift — indirect dependency
- LanguageModelExecutor protocol in LanguageModelExecutor.swift — indirect dependency
- MemoryStore protocol in MemoryStore.swift — consumed by AgentRuntime.memoryStore
- ToolMetadata struct in ToolMetadata.swift — separate from AgentTool protocol; used by ToolEngine at registration time

Existing code for reference (do not modify):
- Sources/SwiftAgentCore/Types/Tool.swift (existing 43-member Tool protocol — the new AgentTool protocol coexists, does not replace)
- Sources/SwiftAgentCore/Agent/QueryEngine.swift (existing agent loop — AgentRuntime supersedes conceptually but no code changes in Phase 1)

Name collision note: The existing `Tool` protocol in Types/Tool.swift prevents naming the new protocol `Tool` (same module = duplicate symbol). Name it `AgentTool` with documentation that it will be renamed to `Tool` in Phase 4 after the old protocol is removed and tools are migrated. The research documents and requirement specs use "Tool" as the conceptual name.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create simplified AgentTool protocol with primary associated type</name>
  <files>
    Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift
  </files>
  <read_first>
    - .planning/phases/01-agentruntime-core-protocols/01-RESEARCH.md (lines 677-705 for Simplified Tool Protocol code example, lines 317-351 for Pattern 4 primary associated type guidance)
    - Sources/SwiftAgentCore/Types/Tool.swift (existing Tool protocol — understand current shape to ensure AgentTool does NOT collide; note the existing `protocol Tool: Sendable` declaration)
    - Sources/SwiftAgentCore/Types/Tool.swift (JSONSchema struct at line 401 — verify it is public, Sendable, suitable for AgentTool.inputSchema)
  </read_first>
  <action>
    Create Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift (TOOL-01):

    Define `public protocol AgentTool<Input>: Sendable` with exactly these members per RESEARCH.md lines 688-705:
    - `associatedtype Input: Codable & Sendable` — the tool's input type
    - `var name: String { get }` — PascalCase tool name (e.g., "Bash", "Read")
    - `var description: String { get }` — human-readable description
    - `var inputSchema: JSONSchema { get }` — JSON Schema for input parameters (uses existing JSONSchema type from Types/Tool.swift)
    - `func call(_ input: Input) async throws -> ToolOutput` — execute with validated input, returns ToolOutput (from plan 01-01)

    This is ~5 members (name, description, inputSchema, call + associatedtype Input + the protocol declaration itself).

    NAMING: The protocol is named `AgentTool` (NOT `Tool`) to avoid collision with the existing `protocol Tool` in `Types/Tool.swift`. Both exist in the same module `SwiftAgentCore`. Add a documentation comment:
    ```
    /// Simplified tool protocol — the contract between model and agent.
    /// ~5 core members. Cross-cutting concerns migrate to ToolMetadata.
    ///
    /// Forward-compatible with WWDC27 AgentIntent auto-discovery.
    ///
    /// - Note: Named `AgentTool` to coexist with the existing `Tool` protocol
    ///   in Types/Tool.swift. Will be renamed to `Tool` in Phase 4 after old
    ///   protocol removal and tool migration.
    ```

    Per RESEARCH.md Pattern 4: Use primary associated type syntax (`protocol AgentTool<Input>`). This ensures `any AgentTool<SomeInput>` existentials preserve Input type information.
    Per RESEARCH.md Pitfall 2 (lines 402-410): Document that bare `any AgentTool` (without angle brackets) loses Input type information. Add doc comment warning.

    The file contains ONLY the AgentTool protocol. It does NOT reference or import the existing Tool protocol. This is a clean, additive definition.
  </action>
  <verify>
    <automated>swift build --disable-sandbox 2>&1 | grep -E "error:.*AgentTool|error:.*duplicate|BUILD"</automated>
  </verify>
  <acceptance_criteria>
    - AgentTool.swift contains `public protocol AgentTool<Input>: Sendable` with primary associated type syntax
    - Protocol has exactly 5 members: `Input` (associatedtype), `name`, `description`, `inputSchema`, `call`
    - `call(_ input: Input) async throws -> ToolOutput` — return type resolves to plan 01-01 ToolOutput
    - `inputSchema: JSONSchema` — type resolves to existing Types/Tool.swift JSONSchema
    - No name collision with existing `Tool` protocol — `swift build` succeeds
    - Doc comment explains AgentTool naming and Phase 4 rename plan
    - Doc comment warns about bare `any AgentTool` losing Input type information
    - `swift build --disable-sandbox` exits 0
  </acceptance_criteria>
  <done>AgentTool protocol defined with ~5 members. Compiles alongside existing Tool protocol without collision. Primary associated type preserves Input type information in existentials.</done>
</task>

<task type="auto">
  <name>Task 2: Create AgentProfile struct and AgentGraph type-slots</name>
  <files>
    Sources/SwiftAgentCore/AgentRuntime/AgentProfile.swift
    Sources/SwiftAgentCore/AgentRuntime/Graph/AgentGraph.swift
  </files>
  <read_first>
    - .planning/phases/01-agentruntime-core-protocols/01-RESEARCH.md (lines 571-623 for AgentProfile + MemoryScope, lines 921-985 for AgentGraph placeholder)
    - Sources/SwiftAgentCore/AgentRuntime/Transcript.swift (from plan 01-01 — MemoryScope enum is defined here; AgentProfile references it)
    - Sources/SwiftAgentCore/AgentRuntime/Providers/PermissionEngine.swift (from plan 01-01 — AgentPermission enum defined here; AgentProfile references it)
    - Sources/SwiftAgentCore/AgentRuntime/Providers/LanguageModel.swift (from plan 01-02 — LanguageModel protocol defined here; AgentProfile references it)
    - Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift (from Task 1 of this plan — AgentTool protocol defined here; AgentProfile references it)
  </read_first>
  <action>
    Create Sources/SwiftAgentCore/AgentRuntime/AgentProfile.swift (RUNTIME-06):
    - Define `public struct AgentProfile: Sendable` with exactly these properties per RESEARCH.md lines 578-612:
      - `public var name: String` — display name for this agent configuration
      - `public var instructions: String` — system instructions defining agent behavior
      - `public var tools: [any AgentTool]` — tools available to this agent (AgentTool from Task 1, NOT the old Tool)
      - `public var model: (any LanguageModel)?` — model for inference (nil = use AgentRuntime default); LanguageModel from plan 01-02
      - `public var permissionMode: AgentPermission` — permission mode; AgentPermission from plan 01-01
      - `public var memoryScope: MemoryScope` — memory persistence boundary; MemoryScope from plan 01-01
    - Provide a memberwise initializer with defaults: `tools: [any AgentTool] = []`, `model: (any LanguageModel)? = nil`, `permissionMode: AgentPermission = .default`, `memoryScope: MemoryScope = .session`
    - Include documentation comment: "Forward-compatible with WWDC27 DynamicProfile runtime switching."
    - MemoryScope is already defined in Transcript.swift (plan 01-01). Do NOT redefine it here — just reference it.

    Create Sources/SwiftAgentCore/AgentRuntime/Graph/AgentGraph.swift (GRAPH-01):
    - Define `public protocol AgentGraph: Sendable` with:
      - `var nodes: [any AgentNode] { get }` — nodes in topological order
      - `func validate() throws` — validate graph structure (no cycles, all inputs satisfied)
    - Define `public protocol AgentNode: Sendable` with:
      - `var agent: AgentProfile { get }` — the agent profile for this node
      - `var inputs: [NodeInput] { get }` — inputs expected by this node
      - `var outputs: [NodeOutput] { get }` — outputs produced by this node
      - `var condition: NodeCondition? { get }` — optional execution condition
    - Define `public struct NodeInput: Sendable` with: `let name: String`, `let type: NodeIOType`
    - Define `public struct NodeOutput: Sendable` with: `let name: String`, `let type: NodeIOType`
    - Define `public enum NodeIOType: Sendable` with cases: `string`, `json`, `transcript`, `toolOutput`
    - Define `public enum NodeCondition: Sendable` with cases: `always`, `whenPresent(String)`, `expression(String)`
    - Per RESEARCH.md lines 927-985: ALL of these are TYPE-SLOTS ONLY — not implemented. Include documentation comment on every type: "TYPE-SLOT ONLY — not implemented. Protocol surface reserved for WWDC27 AgentGraph / WorkflowGraph. No execution logic."
    - All types follow exact RESEARCH.md code example shapes. No additional members.

    Both files are NEW. All types are `public` and `Sendable`.
  </action>
  <verify>
    <automated>swift build --disable-sandbox 2>&1 | grep -E "error:|warning:|BUILD"</automated>
  </verify>
  <acceptance_criteria>
    - AgentProfile.swift contains `public struct AgentProfile: Sendable` with 6 properties (name, instructions, tools, model, permissionMode, memoryScope)
    - `tools` type is `[any AgentTool]` (references new protocol, not old Tool)
    - `model` type is `(any LanguageModel)?` (references plan 01-02 protocol)
    - `permissionMode` type is `AgentPermission` (references plan 01-01 enum)
    - `memoryScope` type is `MemoryScope` (references plan 01-01 enum in Transcript.swift)
    - AgentGraph.swift contains: AgentGraph protocol, AgentNode protocol, NodeInput struct, NodeOutput struct, NodeIOType enum, NodeCondition enum
    - All AgentGraph types have "TYPE-SLOT ONLY" doc comments
    - `NodeCondition` has 3 cases: `always`, `whenPresent(String)`, `expression(String)`
    - AgentProfile init has sensible defaults for tools ([]), model (nil), permissionMode (.default), memoryScope (.session)
    - `swift build --disable-sandbox` exits 0
  </acceptance_criteria>
  <done>AgentProfile identity bundle defined with forward-compatible DynamicProfile shape. AgentGraph + AgentNode type-slots defined (design only, no implementation). All cross-plan type references resolve.</done>
</task>

<task type="auto">
  <name>Task 3: Create AgentRuntime actor protocol with subsystem property slots</name>
  <files>
    Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift
  </files>
  <read_first>
    - .planning/phases/01-agentruntime-core-protocols/01-RESEARCH.md (lines 188-237 for Pattern 1 Actor Protocol with Subsystem Slots)
    - Sources/SwiftAgentCore/AgentRuntime/Providers/LanguageModel.swift (from plan 01-02 — LanguageModel protocol)
    - Sources/SwiftAgentCore/AgentRuntime/Providers/MemoryStore.swift (from plan 01-02 — MemoryStore protocol)
    - Sources/SwiftAgentCore/AgentRuntime/Providers/PermissionEngine.swift (from plan 01-01 — PermissionEngine protocol)
    - Sources/SwiftAgentCore/AgentRuntime/Graph/AgentGraph.swift (from Task 2 — AgentGraph protocol)
    - Sources/SwiftAgentCore/Agent/QueryEngine.swift (existing agent loop — understand current architecture to ensure AgentRuntime supersedes correctly in concept)
  </read_first>
  <action>
    Create Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift (RUNTIME-01):

    First, define 4 placeholder subsystem protocols (minimal type-slots for subsystems that exist conceptually but lack full protocol definitions in Phase 1). These are consumed by AgentRuntime property slots:

    - `public protocol ToolEngine: Sendable` — placeholder for tool registry and execution engine. Minimal definition: no required members in Phase 1 (just the protocol existence). Doc comment: "Tool registry and execution engine. Placeholder protocol — full definition in Phase 2."
    - `public protocol ContextManager: Sendable` — placeholder for context window management and compaction. Doc comment: "Context window management and compaction. Placeholder."
    - `public protocol ProfileManager: Sendable` — placeholder for agent identity/profile management. Doc comment: "Agent identity and profile management. Placeholder."
    - `public protocol HookSystem: Sendable` — placeholder for lifecycle event hooks. Doc comment: "Hook system for lifecycle events. Placeholder."

    Then, define the central protocol per RESEARCH.md lines 212-237:

    `public protocol AgentRuntime: Actor` with exactly these property requirements:
    - `var modelProvider: any LanguageModel { get }` — language model provider for inference (plan 01-02)
    - `var memoryStore: any MemoryStore { get }` — persistent memory store (plan 01-02)
    - `var permissionEngine: any PermissionEngine { get }` — runtime-level access control (plan 01-01)
    - `var toolEngine: any ToolEngine { get }` — tool registry and execution engine (placeholder, this file)
    - `var contextManager: any ContextManager { get }` — context window management (placeholder, this file)
    - `var profileManager: any ProfileManager { get }` — agent identity management (placeholder, this file)
    - `var graphEngine: (any AgentGraph)? { get }` — agent graph orchestration (optional, plan 01-03 Task 2)
    - `var hookSystem: any HookSystem { get }` — lifecycle event hooks (placeholder, this file)

    Per RESEARCH.md Pattern 1:
    - Use `protocol AgentRuntime: Actor` — only `actor` types can conform. All property accesses are actor-isolated.
    - Include documentation comment: "Central agent runtime orchestrator. Only actor types can conform — the compiler enforces actor isolation across all subsystem access. Replaces QueryEngine + LLMClient as the primary consumer API surface."
    - Per RESEARCH.md Pitfall 1 (lines 392-400): Add doc comment explaining the access pattern — properties require `await` to cross the actor boundary, but returned protocol values are locally accessible.
    - Do NOT add methods like `respond(to:)` or `streamResponse(to:)` — those are Phase 2 concerns. Phase 1 defines only the property slots.

    All types are `public` and `Sendable` (AgentRuntime is Actor, which inherits Sendable).
  </action>
  <verify>
    <automated>swift build --disable-sandbox 2>&1 | grep -E "error:|warning:|BUILD"</automated>
  </verify>
  <acceptance_criteria>
    - AgentRuntime.swift contains `public protocol AgentRuntime: Actor` with exactly 8 property requirements
    - Property types: `modelProvider: any LanguageModel`, `memoryStore: any MemoryStore`, `permissionEngine: any PermissionEngine`, `toolEngine: any ToolEngine`, `contextManager: any ContextManager`, `profileManager: any ProfileManager`, `graphEngine: (any AgentGraph)?`, `hookSystem: any HookSystem`
    - 4 placeholder protocols defined: ToolEngine, ContextManager, ProfileManager, HookSystem (all `Sendable`, all empty/minimal)
    - Doc comment on AgentRuntime explains actor isolation, access pattern (await + local access), and relationship to QueryEngine/LLMClient
    - No methods on AgentRuntime (Phase 2 adds respond/streamResponse)
    - `swift build --disable-sandbox` exits 0
    - All cross-plan type references resolve: LanguageModel, MemoryStore, PermissionEngine, AgentGraph
  </acceptance_criteria>
  <done>AgentRuntime actor protocol defined with 8 subsystem property slots + 4 placeholder protocols. Complete AgentRuntime blueprint in place. All 16 Phase 1 types defined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentTool.call() → ToolOutput | Tool execution boundary. Input is Codable-validated. Output is typed ToolOutput (string or blocks). No raw ContentBlock exposure. |
| AgentProfile.tools: [any AgentTool] | Tool array is validated against ToolEngine registry at runtime (Phase 2+). In Phase 1: type-level contract only. |
| AgentRuntime ↔ subsystems | Actor protocol boundary. All subsystem access is actor-isolated. Property reads cross the actor boundary via `await`. |
| AgentGraph.validate() → throws | Graph structure validation — may throw AgentRuntimeError.cycleDetected. Type-slot only in Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | AgentProfile.tools | mitigate | `[any AgentTool]` is immutable after initialization. Tools registered through ToolEngine, not ad-hoc injection. |
| T-01-10 | Elevation of Privilege | AgentRuntime (actor) | mitigate | `protocol AgentRuntime: Actor` — only actors can conform. Compiler enforces actor isolation. No bypass possible without unsafe opt-outs. |
| T-01-11 | Information Disclosure | AgentGraph nodes | accept | AgentGraph is a type-slot only (no execution). Phase 1 defines structure; data flow is Phase 4+ concern. |
| T-01-12 | Denial of Service | AgentTool.call() | accept | Tool execution is a protocol requirement — actual timeout/cancellation is ToolEngine's responsibility (Phase 2+). |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 1 adds zero external packages. No supply-chain risk. |
</threat_model>

<verification>
Phase-level checks for plan 01-03 (final plan of Phase 1):
- `swift build --disable-sandbox` exits 0 (all 14 AgentRuntime files compile together with existing code)
- `swift test --disable-sandbox --no-parallel` exits 0 (all 258+ existing tests pass unchanged)
- `git diff --name-only` confirms only new files created under AgentRuntime/
- All 16 requirements addressed across 3 plans (RUNTIME-01 through GRAPH-01)
- `nonisolated(unsafe)` count remains at 4 (zero new)
- All types are `public` access level
</verification>

<success_criteria>
1. 4 new Swift files created: AgentTool.swift, AgentProfile.swift, AgentGraph.swift, AgentRuntime.swift
2. AgentTool protocol with ~5 members (Input, name, description, inputSchema, call) coexists with existing Tool protocol
3. AgentTool uses primary associated type syntax — `any AgentTool<SomeInput>` existentials preserve Input type
4. AgentProfile struct with 6 fields (name, instructions, tools, model, permissionMode, memoryScope)
5. AgentGraph protocol + AgentNode protocol + NodeInput/Output/Condition/IOType (all type-slots, design only)
6. AgentRuntime actor protocol with 8 property slots (modelProvider, memoryStore, permissionEngine, toolEngine, contextManager, profileManager, graphEngine, hookSystem)
7. 4 placeholder subsystem protocols: ToolEngine, ContextManager, ProfileManager, HookSystem
8. Zero existing files modified; zero new unsafe annotations; all 258+ tests pass
</success_criteria>

<output>
Create `.planning/phases/01-agentruntime-core-protocols/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

### New Files

| File | Symbols |
|------|---------|
| `Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift` | `protocol AgentTool<Input>: Sendable` |
| `Sources/SwiftAgentCore/AgentRuntime/AgentProfile.swift` | `struct AgentProfile: Sendable` |
| `Sources/SwiftAgentCore/AgentRuntime/Graph/AgentGraph.swift` | `protocol AgentGraph: Sendable`, `protocol AgentNode: Sendable`, `struct NodeInput: Sendable`, `struct NodeOutput: Sendable`, `enum NodeIOType: Sendable`, `enum NodeCondition: Sendable` |
| `Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift` | `protocol AgentRuntime: Actor`, `protocol ToolEngine: Sendable`, `protocol ContextManager: Sendable`, `protocol ProfileManager: Sendable`, `protocol HookSystem: Sendable` |

### New Protocols

| Protocol | File | Purpose |
|----------|------|---------|
| `AgentTool<Input>` | AgentTool.swift | Simplified model-tool contract (~5 members); forward-compatible with AgentIntent |
| `AgentRuntime` | AgentRuntime.swift | Central actor protocol — owns all subsystem references; replaces QueryEngine+LLMClient |
| `AgentGraph` | AgentGraph.swift | Ordered DAG of agent nodes (TYPE-SLOT ONLY) |
| `AgentNode` | AgentGraph.swift | Single node in agent graph (TYPE-SLOT ONLY) |
| `ToolEngine` | AgentRuntime.swift | Tool registry and execution engine (placeholder) |
| `ContextManager` | AgentRuntime.swift | Context window management and compaction (placeholder) |
| `ProfileManager` | AgentRuntime.swift | Agent identity and profile management (placeholder) |
| `HookSystem` | AgentRuntime.swift | Lifecycle event hooks (placeholder) |

### New Structs

| Struct | Purpose |
|--------|---------|
| `AgentProfile` | Agent identity bundle (name, instructions, tools, model, permissionMode, memoryScope) |
| `NodeInput` | Input to an agent graph node (name + type) |
| `NodeOutput` | Output from an agent graph node (name + type) |

### New Enums

| Enum | Cases | Purpose |
|------|-------|---------|
| `NodeIOType` | 4 (string, json, transcript, toolOutput) | Type of a graph node input/output |
| `NodeCondition` | 3 (always, whenPresent, expression) | Condition for graph node execution |

### Cross-Phase Consolidated Artifact Summary (All Plans)

This plan (01-03) completes Phase 1. Combined with plans 01-01 and 01-02, Phase 1 produces:

- **14 new Swift files** under `Sources/SwiftAgentCore/AgentRuntime/`
- **14 protocols** (AgentRuntime ← Actor, LanguageModel, LanguageModelExecutor, MemoryStore, AgentStateProtocol, PermissionEngine, GenerationChannel, AgentTool, AgentGraph, AgentNode, ToolEngine, ContextManager, ProfileManager, HookSystem)
- **9 structs** (LanguageModelCapabilities, ToolDefinition, GenerationOptions, MemoryEntry, ToolMetadata, Transcript, OutputBlock, Usage, AgentProfile, NodeInput, NodeOutput)
- **8 enums** (AgentRuntimeError, Transcript.Entry, MemoryScope, AgentPermission, ToolOutput, OutputBlockType, InterruptBehavior, NodeIOType, NodeCondition)
- **0 existing files modified**
- **258+ existing tests pass unchanged**
