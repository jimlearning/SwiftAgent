---
phase: 04-migration-wiring-cleanup
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - Sources/SwiftAgentCore/Types/Tool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/FileWriteTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/FileEditTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/NotebookEditTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/SnipTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/BashTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/LSPTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/PowerShellTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/TerminalCaptureTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/TungstenTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/Batch23ToolRegistry.swift
autonomous: true
requirements:
  - MIG-01
  - MIG-03

must_haves:
  truths:
    - "Types/Tool.swift no longer contains the old Tool protocol, ToolUseContext, ToolResult, or any tool-specific types"
    - "All 9 Batch 2+3 tools compile as RuntimeAgentTool conformers in AgentRuntime/Tools/"
    - "Old tool files for Batches 1-3 are deleted from Sources/SwiftAgentCore/Tools/"
    - "Types/Tool.swift retains only shared types that are NOT deprecated: CanUseToolFn, ToolProgress, ToolProgressData, ToolCallProgress (if still used), SearchOrReadResult (if still used), ValidationResult (if still used)"
  artifacts:
    - path: "Sources/SwiftAgentCore/Types/Tool.swift"
      provides: "Trimmed file — only shared types, no old Tool protocol"
      max_lines: 400
    - path: "Sources/SwiftAgentCore/AgentRuntime/Tools/BashTool.swift"
      provides: "BashTool: RuntimeAgentTool"
      min_lines: 60
  key_links:
    - from: "BashTool.call(arguments:)"
      to: "self.workingDirectory + self.shell"
      via: "captured at init per D-03"
      pattern: "self\\.(workingDirectory|shell)"
    - from: "Each Batch 2+3 tool's Arguments struct"
      to: "CodingKeys enum"
      via: "snake_case JSON keys from old inputSchema"
      pattern: "case.*=.*\".*_.*\""
---

<objective>
Delete the old Tool protocol and ToolUseContext from Types/Tool.swift per D-06, then migrate 9 file-mutation and command-execution tools (Batches 2+3) to RuntimeAgentTool. Delete old tool files for Batches 1, 2, and 3 from Sources/SwiftAgentCore/Tools/. This is the forcing-function plan: after the old protocol is deleted, the remaining 39 unmigrated tools will fail to compile, forcing their migration in Plan 04-04.

Purpose: D-06 mandates deletion of the old Tool protocol after Batch 1 validates the pattern. This removes dual-maintenance pressure and forces complete migration. Batches 2+3 cover file mutation tools (first destructive tools) and command execution tools (highest-risk category).
Output: Types/Tool.swift gutted (only shared utility types remain), 9 new RuntimeAgentTool files created, 24 old tool files deleted (15 from batch 1 + 4 from batch 2 + 5 from batch 3).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-migration-wiring-cleanup/04-CONTEXT.md
@.planning/phases/04-migration-wiring-cleanup/04-RESEARCH.md
@.planning/REQUIREMENTS.md

# Pattern reference
@Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift
@Sources/SwiftAgentCore/AgentRuntime/Tools/ToolMetadata.swift

# Old tool files to migrate
@Sources/SwiftAgentCore/Tools/FileWriteTool.swift
@Sources/SwiftAgentCore/Tools/FileEditTool.swift
@Sources/SwiftAgentCore/Tools/NotebookEditTool.swift
@Sources/SwiftAgentCore/Tools/SnipTool.swift
@Sources/SwiftAgentCore/Tools/BashTool.swift
@Sources/SwiftAgentCore/Tools/LSPTool.swift
@Sources/SwiftAgentCore/Tools/PowerShellTool.swift
@Sources/SwiftAgentCore/Tools/TerminalCaptureTool.swift
@Sources/SwiftAgentCore/Tools/TungstenTool.swift

# File to gut
@Sources/SwiftAgentCore/Types/Tool.swift

# Batch 1 registry (pattern reference for Batch23ToolRegistry)
@Sources/SwiftAgentCore/AgentRuntime/Tools/Batch1ToolRegistry.swift

# JSONSchema types (already extracted in 04-01 — verify they're in standalone files)
@Sources/SwiftAgentCore/Types/JSONSchema.swift
@Sources/SwiftAgentCore/Types/InterruptBehavior.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old Tool protocol and ToolUseContext from Types/Tool.swift per D-06</name>
  <files>
    Sources/SwiftAgentCore/Types/Tool.swift
  </files>
  <read_first>
    Sources/SwiftAgentCore/Types/Tool.swift

    Purpose: Read the complete file (1164 lines) to identify ALL type definitions. Must know which types are deprecated (delete) vs. shared (keep).

    Sources/SwiftAgentCore/Types/JSONSchema.swift
    Purpose: Confirm JSONSchema types are already in standalone file (extracted in 04-01) so they can be safely absent from Tool.swift.

    Sources/SwiftAgentCore/Types/InterruptBehavior.swift
    Purpose: Confirm InterruptBehavior is already extracted.
  </read_first>
  <action>
    Gut Types/Tool.swift per D-06. Delete the old Tool protocol and ToolUseContext. The Research section "Pitfall 6: SPM Target Dependency Confusion" warns that multiple type definitions cohabitate in this file. Must surgically remove only deprecated types while preserving shared types that are NOT being deprecated.

    Step A — Identify what to DELETE (deprecated, replaced by RuntimeAgentTool and ToolMetadata):

    1. **Old Tool protocol** (line ~43-289): All members including the 30+ method/property requirements. This is THE forcing function — after deletion, 48 remaining old Tool files will fail to compile.
    2. **ToolUseContext struct** (line ~543-800+): The 70+ field context struct. No longer needed per D-03 (context injected at tool init).
    3. **ToolResult enum** (line ranges TBD — read the file): Replaced by ToolOutputValue.
    4. **ToolOutput** discriminated union (line ranges TBD): Replaced by ToolOutputValue.
    5. **ToolDefinition struct** (if present in this file): Replaced by SessionToolDefinition.
    6. **ToolResultBlockParam struct** (if present): Replaced by OutputBlock.
    7. **ToolInput typealias** (if present): Replaced by typed Arguments structs.
    8. **ToolRegistry** (if present in this file — check): Replaced by ToolEngine/DefaultToolEngine.
    9. Any other type whose only purpose is serving the old Tool protocol.

    Step B — Identify what to KEEP (shared types, still referenced by other code):

    1. **CanUseToolFn** (line ~10): Permission callback type. Check if still referenced by PermissionEngine or other non-Tool code. If only referenced by old Tool protocol and ToolUseContext, safe to delete. Grep before deciding: `grep -rn 'CanUseToolFn' Sources/` excluding Types/Tool.swift.
    2. **ToolProgressData** (line ~21): Progress data protocol. Check references.
    3. **ToolProgress** (line ~27): Progress event struct. Check references.
    4. **ToolCallProgress** (line ~39): Progress callback type. Check references.
    5. **SearchOrReadResult** (line ranges TBD): Used by CLI rendering for collapse detection. May be needed in Plan 04-05 (CLI rewiring). Grep: `grep -rn 'SearchOrReadResult' Sources/`.
    6. **ValidationResult** (if present): Validation result type. Check if used by non-Tool code.
    7. **ToolDescriptionOptions** (if present): Description options struct. Check references.
    8. **MCPToolInfo** (if present): MCP tool info type. Check references.

    Decision rule for each "keep candidate": if `grep -rn 'TypeName' Sources/` returns matches ONLY in Types/Tool.swift and old Tools/*.swift and old Agent/*.swift (all being deleted) → DELETE the type. If it returns matches in AgentRuntime/ or Safety/ or CLI/ (non-deleted code) → KEEP the type in Tool.swift.

    Step C — Execute deletion:

    1. Read Types/Tool.swift line by line to identify exact line ranges for each type.
    2. Delete the old Tool protocol (define by its `protocol Tool: Sendable {` header through the closing `}`).
    3. Delete ToolUseContext (from `struct ToolUseContext` through its closing `}`).
    4. Delete ToolResult, ToolOutput, ToolDefinition, ToolResultBlockParam, ToolInput, and any other deprecated-only types.
    5. Delete associated MARK comments for removed sections.
    6. Keep only the remaining shared types (if any from Step B).

    Step D — Update Tool.swift header comment:

    If the file header doc comment says "core Tool protocol" or similar, update it to reflect the file's new contents (shared tool-related utility types only, no protocol).

    Step E — Verify shared types accessibility:

    After deletion, verify that any kept types (InterruptBehavior, JSONSchema) are accessible. Since JSONSchema was already extracted to Types/JSONSchema.swift in Plan 04-01, Tool.swift doesn't need to provide it. But InterruptBehavior was also extracted. So Tool.swift may be nearly empty or contain only CanUseToolFn-like callback types. If Tool.swift becomes empty (all types deleted), delete the file entirely.

    CRITICAL: Do NOT delete JSONSchema types — they were already extracted in 04-01. Do NOT delete InterruptBehavior — it was already extracted. These types are now in standalone files. Tool.swift should NOT contain them (they were removed in 04-01 task 2).

    POST-DELETION STATE: 48 old tool files in Sources/SwiftAgentCore/Tools/ will fail to compile because their `: Tool` conformance references a now-deleted protocol. This is intentional per D-06 — these tools MUST be migrated in Plans 04-03 (tasks 2+3) and 04-04.
  </action>
  <acceptance_criteria>
    - Source assertion: `grep -c 'protocol Tool: Sendable' Sources/SwiftAgentCore/Types/Tool.swift` returns 0
    - Source assertion: `grep -c 'struct ToolUseContext' Sources/SwiftAgentCore/Types/Tool.swift` returns 0
    - Source assertion: `grep -c 'protocol Tool\b' Sources/SwiftAgentCore/Types/Tool.swift` returns 0 (no old Tool protocol definition — `\b` avoids matching RuntimeAgentTool references)
    - Source assertion: `grep -c 'enum ToolResult' Sources/SwiftAgentCore/Types/Tool.swift` returns 0 (if ToolResult was in this file)
    - Source assertion: `grep -c 'enum ToolOutput ' Sources/SwiftAgentCore/Types/Tool.swift` returns 0 (if ToolOutput discriminated union was in this file — note trailing space to distinguish from ToolOutputValue)
    - Build: The Core target will NOT compile fully yet — 48 old tool files reference the deleted protocol. This is expected and intentional. Verify the deletion itself is correct by checking that non-Tool code compiles: `swift build --disable-sandbox --target SwiftAgentCore 2>&1 | grep -c "error:"` — errors are expected and acceptable at this stage
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -c "protocol Tool: Sendable" Sources/SwiftAgentCore/Types/Tool.swift; grep -c "struct ToolUseContext" Sources/SwiftAgentCore/Types/Tool.swift'</automated>
  </verify>
  <done>Types/Tool.swift no longer contains old Tool protocol or ToolUseContext. Shared types (if any) retained. 48 old tool files now have compile errors — this is the intended forcing function per D-06.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Batch 2 — 4 file mutation tools to RuntimeAgentTool</name>
  <files>
    Sources/SwiftAgentCore/AgentRuntime/Tools/FileWriteTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/FileEditTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/NotebookEditTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/SnipTool.swift
  </files>
  <read_first>
    Sources/SwiftAgentCore/Tools/FileWriteTool.swift
    Purpose: Understand the tool's call() logic, context field accesses, input schema, and ToolResult usage.

    Sources/SwiftAgentCore/Tools/FileEditTool.swift
    Purpose: Same — FileEditTool has specific edit patterns (string replacement, line-based editing).

    Sources/SwiftAgentCore/Tools/NotebookEditTool.swift
    Purpose: Notebook-specific editing with cell manipulation.

    Sources/SwiftAgentCore/Tools/SnipTool.swift
    Purpose: Code snippet capture/insertion.

    Sources/SwiftAgentCore/AgentRuntime/Tools/FileReadTool.swift
    Purpose: Reference the Batch 1 pattern (read-only) — specifically how workingDirectory is captured at init and used in call(arguments:).
  </read_first>
  <action>
    Migrate 4 file mutation tools using the same pattern established in Plan 04-02 Task 1. Key difference: these tools are destructive (isReadOnly: false) but follow identical structural patterns.

    For each tool, following the Batch 1 migration template:

    **FileWriteTool** (Sources/SwiftAgentCore/Tools/FileWriteTool.swift, 124 lines):
    - Context: workingDirectory (implicit via path resolution in old tool)
    - Init: `let workingDirectory: String`
    - Arguments: filePath (String), content (String)
    - call(arguments:): Write content to resolved path. Resolve relative paths against self.workingDirectory.
    - inputSchema: Preserve exact schema from old file

    **FileEditTool** (Sources/SwiftAgentCore/Tools/FileEditTool.swift, 211 lines):
    - Context: workingDirectory
    - Init: `let workingDirectory: String`
    - Arguments: filePath (String), oldString (String), newString (String), replaceAll (Bool?)
    - call(arguments:): String replacement / line editing logic. Resolve paths against self.workingDirectory.
    - inputSchema: Preserve exact schema

    **NotebookEditTool** (Sources/SwiftAgentCore/Tools/NotebookEditTool.swift, 210 lines):
    - Context: none apparent from RESEARCH.md
    - Init: no context params (verify by grepping old file for `context.`)
    - Arguments: notebookPath, cellOperations (read old schema for exact fields)
    - call(arguments:): Cell-level editing for Jupyter notebooks
    - inputSchema: Preserve exact schema

    **SnipTool** (Sources/SwiftAgentCore/Tools/SnipTool.swift, 26 lines):
    - Context: none apparent
    - Init: no context params
    - Arguments: read old inputSchema for exact fields (very small tool — 26 lines)
    - call(arguments:): Snip logic
    - inputSchema: Preserve exact schema

    For each tool:
    1. Grep old file for `context.` to verify context field usage (RESEARCH.md analysis may miss private helper accesses)
    2. Capture all accessed context fields as `let` properties at init
    3. Create typed Arguments: Codable struct with CodingKeys
    4. Preserve inputSchema JSON shape exactly
    5. Convert ToolResult → ToolOutputValue returns
    6. Convert any thrown errors to AgentRuntimeError cases
    7. Remove validateInput() → inline validation at top of call(arguments:)
    8. Remove canUseTool/parentMessage/onProgress parameters
    9. Write to Sources/SwiftAgentCore/AgentRuntime/Tools/

    After all 4 tools are created, DELETE the old files:
    - Sources/SwiftAgentCore/Tools/FileWriteTool.swift
    - Sources/SwiftAgentCore/Tools/FileEditTool.swift
    - Sources/SwiftAgentCore/Tools/NotebookEditTool.swift
    - Sources/SwiftAgentCore/Tools/SnipTool.swift
  </action>
  <acceptance_criteria>
    - Exist: `ls Sources/SwiftAgentCore/AgentRuntime/Tools/{FileWrite,FileEdit,NotebookEdit,Snip}Tool.swift 2>/dev/null | wc -l` returns 4
    - Conformance: `grep -l 'RuntimeAgentTool' Sources/SwiftAgentCore/AgentRuntime/Tools/{FileWrite,FileEdit,NotebookEdit,Snip}Tool.swift | wc -l` returns 4
    - No old context: `grep -l 'ToolUseContext\|ToolResult\b' Sources/SwiftAgentCore/AgentRuntime/Tools/{FileWrite,FileEdit,NotebookEdit,Snip}Tool.swift` returns 0 matches (empty result)
    - Deleted: `ls Sources/SwiftAgentCore/Tools/{FileWrite,FileEdit,NotebookEdit,Snip}Tool.swift 2>&1` returns "No such file or directory" for each
  </acceptance_criteria>
  <verify>
    <automated>ls Sources/SwiftAgentCore/AgentRuntime/Tools/{FileWrite,FileEdit,NotebookEdit,Snip}Tool.swift 2>/dev/null | wc -l</automated>
  </verify>
  <done>4 file mutation tools compiled as RuntimeAgentTool in AgentRuntime/Tools/. Old files deleted from Tools/. ToolUseContext and ToolResult no longer referenced.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate Batch 3 — 5 command execution tools + delete old Batch 1 files + create Batch23ToolRegistry</name>
  <files>
    Sources/SwiftAgentCore/AgentRuntime/Tools/BashTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/LSPTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/PowerShellTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/TerminalCaptureTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/TungstenTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/Batch23ToolRegistry.swift
  </files>
  <read_first>
    Sources/SwiftAgentCore/Tools/BashTool.swift
    Purpose: Largest and riskiest tool (597 lines). Understand Process execution, timeout logic, sandbox, shell configuration, working directory handling. Grep for `context.` to verify shell and workingDirectory field usage.

    Sources/SwiftAgentCore/Tools/LSPTool.swift
    Purpose: LSP server interaction tool (378 lines). Understand how it connects to language servers, what context it needs.

    Sources/SwiftAgentCore/AgentRuntime/Tools/Batch1ToolRegistry.swift
    Purpose: Pattern reference for creating Batch23ToolRegistry with matching structure.
  </read_first>
  <action>
    Migrate the 5 command execution tools (Batch 3) and create their registry. Also delete the old Batch 1 tool files from Sources/SwiftAgentCore/Tools/ (now that Batch 1 migration is validated per D-06).

    PART A — Migrate 5 command execution tools:

    **BashTool** (Sources/SwiftAgentCore/Tools/BashTool.swift, 597 lines, HIGHEST-RISK TOOL):
    - Context: workingDirectory, shell (per RESEARCH.md — verify with `grep 'context\.'`
    - Init: `let workingDirectory: String`, `let shell: String`
    - Arguments: command (String), timeout (Int?), dangerouslyDisableSandbox (Bool?), workdir (String? — overrides init workingDirectory)
    - call(arguments:): Execute shell command via Process. Use self.shell (default "/bin/zsh") and self.workingDirectory. Preserve the timeout logic (TaskGroup race between Process.waitUntilExit and Task.sleep). Preserve sandbox mode and danger pattern detection.
    - inputSchema: Preserve the exact BashTool JSON schema — this is a critical contract with the LLM
    - WARNING: BashTool has `dangerouslyDisableSandbox` flag and complex Process management. Do NOT simplify the execution logic — reproduce the full behavior from the old implementation.

    **LSPTool** (Sources/SwiftAgentCore/Tools/LSPTool.swift, 378 lines):
    - Context: workingDirectory
    - Init: `let workingDirectory: String`
    - Arguments: Read old inputSchema for exact fields (likely: operation, server, file, line, column, etc.)
    - call(arguments:): LSP server interaction. Resolve project paths against self.workingDirectory.
    - inputSchema: Preserve exact schema

    **PowerShellTool** (Sources/SwiftAgentCore/Tools/PowerShellTool.swift, 47 lines):
    - Context: none apparent
    - Init: no context params
    - Arguments: command, dangerouslyDisableSandbox (matching old schema)
    - call(arguments:): PowerShell execution (Windows-specific, likely stubbed on macOS)
    - inputSchema: Preserve exact schema

    **TerminalCaptureTool** (Sources/SwiftAgentCore/Tools/TerminalCaptureTool.swift, 25 lines):
    - Context: none apparent
    - Init: no context params
    - Arguments: Read old schema
    - call(arguments:): Terminal output capture logic
    - inputSchema: Preserve exact schema

    **TungstenTool** (Sources/SwiftAgentCore/Tools/TungstenTool.swift, 25 lines):
    - Context: none apparent
    - Init: no context params
    - Arguments: Read old schema
    - call(arguments:): Tungsten-specific logic
    - inputSchema: Preserve exact schema

    For each tool: follow the same migration pattern as Batch 1. For context fields, grep `context\.` in the old file to verify RESEARCH.md's analysis.

    PART B — Delete old Batch 1 tool files from Sources/SwiftAgentCore/Tools/:

    Delete the following 15 files (their replacements exist in AgentRuntime/Tools/):
    - FileReadTool.swift, GrepTool.swift, GlobTool.swift, WebSearchTool.swift, WebFetchTool.swift
    - ListSkillsTool.swift, ToolSearchTool.swift, ListMcpResourcesTool.swift, ReadMcpResourceTool.swift
    - CtxInspectTool.swift, ListPeersTool.swift, BriefTool.swift
    - TaskGetTool.swift, TaskListTool.swift, BundledSkills.swift

    PART C — Also delete old Batch 2 files (if not already deleted in Task 2):
    - FileWriteTool.swift, FileEditTool.swift, NotebookEditTool.swift, SnipTool.swift

    PART D — Create Batch23ToolRegistry.swift:

    Following the Batch1ToolRegistry pattern, create a registry for Batches 2+3 tools (9 tools total). Each entry pairs the tool with its ToolMetadata:
    - isReadOnly: false for all Batch 2+3 tools
    - requiresApproval: true for destructive tools (file writes, bash commands)
    - isDestructive: true for file mutation and command execution tools
    - interruptBehavior: .cancel for long-running tools (Bash, LSP)
    - activityDescription: derived from old getActivityDescription() overrides
    - searchHint: from old searchHint overrides

    PART E — Update test files:

    The following test files reference ToolUseContext and will have compile errors after the old protocol deletion. For this plan, these test files will remain with compile errors — they will be fixed in Plan 04-06 (Final Cleanup). Document this in the SUMMARY:
    - Phase1TypesTests.swift, Phase2LLMTests.swift, Phase3AgentTests.swift,
    - Phase4ToolsTests.swift, Phase11SubAgentTests.swift
  </action>
  <acceptance_criteria>
    - Exist: `ls Sources/SwiftAgentCore/AgentRuntime/Tools/{Bash,LSP,PowerShell,TerminalCapture,Tungsten}Tool.swift 2>/dev/null | wc -l` returns 5
    - Conformance: `grep -l 'RuntimeAgentTool' Sources/SwiftAgentCore/AgentRuntime/Tools/{Bash,LSP,PowerShell,TerminalCapture,Tungsten}Tool.swift | wc -l` returns 5
    - BashTool context: `grep -c 'self\.shell\|self\.workingDirectory' Sources/SwiftAgentCore/AgentRuntime/Tools/BashTool.swift` returns at least 2
    - No old context types: `grep -l 'ToolUseContext\|ToolResult\b' Sources/SwiftAgentCore/AgentRuntime/Tools/{Bash,LSP,PowerShell,TerminalCapture,Tungsten}Tool.swift` returns 0 matches (empty result)
    - Batch 1 deleted: `ls Sources/SwiftAgentCore/Tools/{FileRead,Grep,Glob,WebSearch,WebFetch,ListSkills,ToolSearch,ListMcpResources,ReadMcpResource,CtxInspect,ListPeers,Brief,TaskGet,TaskList,Bundled}*.swift 2>&1 | grep 'No such file' | wc -l` returns 15
    - Batch 2 deleted: `ls Sources/SwiftAgentCore/Tools/{FileWrite,FileEdit,NotebookEdit,Snip}Tool.swift 2>&1 | grep 'No such file' | wc -l` returns 4
    - Batch 3 deleted: `ls Sources/SwiftAgentCore/Tools/{Bash,LSP,PowerShell,TerminalCapture,Tungsten}Tool.swift 2>&1 | grep 'No such file' | wc -l` returns 5
    - Batch23ToolRegistry: `grep -c 'ToolMetadata(' Sources/SwiftAgentCore/AgentRuntime/Tools/Batch23ToolRegistry.swift` returns 9
    - Total old tool files remaining: `ls Sources/SwiftAgentCore/Tools/*.swift | wc -l` should be 42 (63 total - 15 batch 1 - 4 batch 2 - 5 batch 3 + 3 shared files = wait: 63 regular tool files + 3 shared = 66. After deleting 24 tool files: 66 - 24 = 42 remaining, of which 39 are tool files and 3 are shared types. Actually the shared files are CronTypes, WorktreeTypes, BuiltInAgents. So 42 total: 39 batch 4+5 tool files + 3 shared.)
  </acceptance_criteria>
  <verify>
    <automated>ls Sources/SwiftAgentCore/AgentRuntime/Tools/{Bash,LSP,PowerShell,TerminalCapture,Tungsten}Tool.swift 2>/dev/null | wc -l</automated>
  </verify>
  <done>All 5 command execution tools migrated to RuntimeAgentTool. Batch 1+2+3 old files deleted (24 files). Batch23ToolRegistry created. 39 old tool files remain (will fail to compile — forced migration in 04-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bash Tool execution | Shell commands run via Process — the highest-risk boundary in the system |
| File mutation tools | Write/edit operations on filesystem — permission gating moved to LanguageModelSessionImpl.executeTool() |
| Tool protocol deletion | Removing the old Tool protocol breaks 39 unmigrated tools at compile time — intentional, not a security issue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Elevation of Privilege | BashTool dangerouslyDisableSandbox | mitigate | Flag preserved in Arguments; permissionEngine.check(.runCommands) gates every execution in LanguageModelSessionImpl |
| T-04-05 | Tampering | FileWriteTool path traversal | mitigate | workingDirectory scoping + FileManager canonicalization preserved from old implementation |
| T-04-06 | Denial of Service | BashTool timeout | mitigate | TaskGroup race between Process.waitUntilExit and Task.sleep preserved from old BashTool |
| T-04-07 | Spoofing | Old Tool protocol deletion | accept | Intentional compile-time failure for unmigrated tools — forces migration per D-06. No runtime risk. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed in this phase |
</threat_model>

<verification>
1. Types/Tool.swift no longer contains `protocol Tool: Sendable` or `struct ToolUseContext`
2. All 9 Batch 2+3 tool files exist in AgentRuntime/Tools/ and conform to RuntimeAgentTool
3. 24 old tool files deleted from Sources/SwiftAgentCore/Tools/
4. Batch23ToolRegistry.swift covers all 9 tools with correct metadata
5. Remaining 39 old tool files will have compile errors (intentional — forced migration per D-06)
6. Any shared types in Tool.swift that are still referenced by non-tool code are preserved
</verification>

<success_criteria>
Old Tool protocol and ToolUseContext deleted from Types/Tool.swift per D-06. 9 Batch 2+3 tools migrated to RuntimeAgentTool. 24 old tool files deleted. Remaining 39 tools will fail to compile — forcing their migration in Plan 04-04. Batch23ToolRegistry ready for session construction. The compile breakage is intentional and documented.
</success_criteria>

<output>
Create `.planning/phases/04-migration-wiring-cleanup/04-03-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `FileWriteTool` | struct: RuntimeAgentTool | `AgentRuntime/Tools/FileWriteTool.swift` | File write tool |
| `FileEditTool` | struct: RuntimeAgentTool | `AgentRuntime/Tools/FileEditTool.swift` | File edit tool |
| `NotebookEditTool` | struct: RuntimeAgentTool | `AgentRuntime/Tools/NotebookEditTool.swift` | Notebook edit tool |
| `SnipTool` | struct: RuntimeAgentTool | `AgentRuntime/Tools/SnipTool.swift` | Code snip tool |
| `BashTool` | struct: RuntimeAgentTool | `AgentRuntime/Tools/BashTool.swift` | Shell execution tool |
| `LSPTool` | struct: RuntimeAgentTool | `AgentRuntime/Tools/LSPTool.swift` | LSP interaction tool |
| `PowerShellTool` | struct: RuntimeAgentTool | `AgentRuntime/Tools/PowerShellTool.swift` | PowerShell execution tool |
| `TerminalCaptureTool` | struct: RuntimeAgentTool | `AgentRuntime/Tools/TerminalCaptureTool.swift` | Terminal capture tool |
| `TungstenTool` | struct: RuntimeAgentTool | `AgentRuntime/Tools/TungstenTool.swift` | Tungsten tool |
| `Batch23ToolRegistry.tools(workingDirectory:shell:mcpClients:)` | static func | `AgentRuntime/Tools/Batch23ToolRegistry.swift` | Registry for 9 batch 2+3 tools with metadata |
| 9 new `.swift` files | file | `AgentRuntime/Tools/` | New tool implementations |
| 9 `Arguments` types | struct: Codable, Sendable | within each tool file | Typed input structs |
| 24 deleted old files | deletion | `Sources/SwiftAgentCore/Tools/` | Old batch 1+2+3 tool files removed |
| `Types/Tool.swift` (gutted) | file (modified) | `Sources/SwiftAgentCore/Types/Tool.swift` | Old protocol and ToolUseContext removed |
