---
phase: 04-migration-wiring-cleanup
plan: 06
type: execute
wave: 6
depends_on:
  - 04-05
files_modified:
  - Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift
  - Sources/SwiftAgentApp/Agent/AgentSessionManager.swift
  - Sources/SwiftAgentApp/Agent/AppAgentProvider.swift
  - Sources/SwiftAgentApp/Agent/AgentMessages.swift
  - Sources/SwiftAgentApp/LLM/AnthropicProvider.swift
  - Sources/SwiftAgentApp/LLM/AppLLMProvider.swift
  - Sources/SwiftAgentApp/LLM/DeepSeekProvider.swift
  - Sources/SwiftAgentApp/LLM/LLMProvider.swift
  - Sources/SwiftAgentApp/LLM/OpenAIProvider.swift
  - Sources/SwiftAgentApp/LLM/ProviderRegistry.swift
  - Sources/SwiftAgentApp/DeepSeek/DeepSeekClient.swift
  - Sources/SwiftAgentApp/DeepSeek/DeepSeekConfig.swift
  - Sources/SwiftAgentApp/DeepSeek/DeepSeekModel.swift
  - Sources/SwiftAgentApp/DeepSeek/KeychainStore.swift
  - Sources/SwiftAgentCore/Storage/SwiftAgentStore.swift
  - Sources/SwiftAgentCore/Storage/SessionStore.swift
  - Sources/SwiftAgentCore/Storage/MemoryStore.swift
  - Sources/SwiftAgentCore/Storage/SessionIndexStore.swift
  - Sources/SwiftAgentCore/Storage/ProjectDiscoverer.swift
  - Sources/SwiftAgentCore/Storage/SessionDebugLog.swift
  - Sources/SwiftAgentCore/Storage/SwiftAgentPaths.swift
  - Sources/SwiftAgentCore/Storage/TranscriptStore.swift
  - Sources/SwiftAgentCore/Agent/MessageNormalizer.swift
  - Sources/SwiftAgentCore/Commands/EvalCommand.swift
  - Tests/SwiftAgentCoreTests/Phase1TypesTests.swift
  - Tests/SwiftAgentCoreTests/Phase2LLMTests.swift
  - Tests/SwiftAgentCoreTests/Phase3AgentTests.swift
  - Tests/SwiftAgentCoreTests/Phase4ToolsTests.swift
  - Tests/SwiftAgentCoreTests/Phase9StorageTests.swift
  - Tests/SwiftAgentCoreTests/Phase11SubAgentTests.swift
  - Tests/SwiftAgentCoreTests/MockProviders.swift
  - Tests/SwiftAgentCoreTests/AgentRuntimeImplTests.swift
  - Tests/SwiftAgentCoreTests/AgentPermissionBridgeTests.swift
  - Tests/SwiftAgentCoreTests/AnthropicProviderTests.swift
  - Tests/SwiftAgentCoreTests/DeepSeekProviderTests.swift
  - Tests/SwiftAgentCoreTests/OpenAIProviderTests.swift
  - Tests/SwiftAgentCoreTests/SQLiteMemoryStoreTests.swift
  - Tests/SwiftAgentAppTests/PersistenceRoundTripTests.swift
  - Tests/SwiftAgentCLITests/TerminalRenderingTests.swift
autonomous: true
requirements:
  - MIG-02
  - MIG-03
  - MIG-04
  - MIG-05

must_haves:
  truths:
    - "ThreadViewModel.send() constructs LanguageModelSessionImpl and iterates streamResponse(to:) instead of AgentSessionManager.run()"
    - "SessionEvent cases bridge to AgentMessage blocks (textDelta -> appendText, thinkingDelta -> appendThinking, toolCallRequested -> ToolUseBlock with .executing, toolCallCompleted -> update status + ToolResultBlock)"
    - "App/Agent/, App/LLM/, App/DeepSeek/, Storage/ (8 files), MessageNormalizer.swift, and EvalCommand.swift are all deleted"
    - "Full test suite compiles and passes: swift test --disable-sandbox --no-parallel"
    - "nonisolated(unsafe) count is <= 2 (SwiftAgentPaths.swift deleted, TerminalView closures remain)"
  artifacts:
    - path: "Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift"
      provides: "Rewired ThreadViewModel using LanguageModelSessionImpl"
      contains: "LanguageModelSessionImpl"
  key_links:
    - from: "ThreadViewModel.startAgentRun()"
      to: "LanguageModelSessionImpl.streamResponse(to:)"
      via: "AsyncThrowingStream<SessionEvent>"
      pattern: "streamResponse\\(to:"
    - from: "ThreadViewModel.handleStreamEvent()"
      to: "SessionEvent cases"
      via: "switch on SessionEvent bridging to AgentMessage"
      pattern: "case \\.(textDelta|thinkingDelta|toolCallRequested|toolCallCompleted|turnCompleted|error)"
---

<objective>
Wire the App consumer (ThreadViewModel) to LanguageModelSessionImpl, delete all App-side deprecated types plus Storage/ and MessageNormalizer and EvalCommand, fix all 15 test files referencing deleted types, and verify the full test suite passes. This is the final plan — after it completes, the migration is done.

Purpose: Complete App-side wiring per D-11, remove all remaining deprecated types per D-12/D-13/D-14/D-15/D-16/D-17, and achieve full test pass (MIG-04).
Output: ThreadViewModel using streamResponse(to:), 24+ deprecated files deleted, 15 test files updated, all tests passing.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-migration-wiring-cleanup/04-CONTEXT.md
@.planning/phases/04-migration-wiring-cleanup/04-RESEARCH.md

# Key source files
@Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift
@Sources/SwiftAgentApp/Agent/AgentSessionManager.swift
@Sources/SwiftAgentApp/Agent/AppAgentProvider.swift
@Sources/SwiftAgentApp/Agent/AgentMessages.swift
@Sources/SwiftAgentCore/AgentRuntime/AgentRuntimeImpl.swift
@Sources/SwiftAgentCore/AgentRuntime/SessionEvent.swift
</context>

<open_questions_resolved>
## Resolved Open Questions

1. **AgentMessages extraction:** RESOLVED — grep for `AgentMessage` references across `Sources/SwiftAgentApp/` at execution time. If `AgentMessages.swift` IS the sole definition of `AgentMessage`, extract the type into `Sources/SwiftAgentApp/Types/AgentMessage.swift` before deleting `AgentMessages.swift`. If `AgentMessage` is defined elsewhere (e.g., in ThreadViewModel), skip the extraction and delete `AgentMessages.swift` directly.

2. **KeychainStore deletion safety:** RESOLVED — verify at execution time that `Sources/SwiftAgentCore/AgentRuntime/Providers/` has its own Keychain abstraction (check for Keychain references in AnthropicProvider/DeepSeekProvider/OpenAIProvider). If the providers reference a shared Keychain type, extract the Keychain type from `KeychainStore.swift` into a shared location (e.g., `Sources/SwiftAgentCore/Security/Keychain.swift`) before deleting `App/DeepSeek/KeychainStore.swift`. If providers have their own inline keychain handling, delete `KeychainStore.swift` directly.

3. **SwiftAgentPaths.configHomeOverride references:** RESOLVED — grep for `SwiftAgentPaths.configHomeOverride` and `SwiftAgentPaths ` references across `Sources/` excluding the file itself. If survivors exist (code outside Storage/ that references path utilities), migrate those references to `SQLiteMemoryStore` path configuration before deleting `SwiftAgentPaths.swift`. If no survivors, delete directly.

4. **Provider normalization pass coverage:** RESOLVED — per D-17, normalization lives inside each provider (AnthropicProvider, DeepSeekProvider, OpenAIProvider). Before deleting `MessageNormalizer.swift`, verify each provider's `respond()` method covers the essential passes: message ordering, content block sanitization, tool use/result pairing, and thinking block insertion. If any pass is missing from a provider, add it to that specific provider — do NOT create a shared TranscriptNormalizer. Delete `MessageNormalizer.swift` only after all three providers cover their essential passes.
</open_questions_resolved>

<tasks>

<task type="auto">
  <name>Task 1: Rewire ThreadViewModel to LanguageModelSessionImpl.streamResponse(to:) per D-11</name>
  <files>
    Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift
  </files>
  <read_first>
    Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift — offset 70, limit 40
    Purpose: Read dependency declarations — agentSession (AgentSessionManager?), store (SwiftAgentStore?), workingDirectory getter.

    Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift — offset 251, limit 150
    Purpose: Read send() and startAgentRun() — AgentSessionManager.run() call, StreamingQueryEvent callback, conversation building, streamingTask construction.

    Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift — offset 442, limit 100
    Purpose: Read handleStreamEvent() — StreamingQueryEvent switch cases, AgentMessage mutation, persistence.

    Sources/SwiftAgentCore/AgentRuntime/AgentRuntimeImpl.swift
    Purpose: Confirm LanguageModelSessionImpl.init parameters and streamResponse(to:) signature (returns AsyncThrowingStream<SessionEvent, Error>).

    Sources/SwiftAgentCore/AgentRuntime/SessionEvent.swift
    Purpose: All 6 SessionEvent cases to map to AgentMessage block operations.
  </read_first>
  <action>
    Replace ThreadViewModel's AgentSessionManager.run() with LanguageModelSessionImpl.streamResponse(to:) per D-11 (direct replacement, no feature flags).

    **The core replacement:**
    Delete `private weak var agentSession: AgentSessionManager?` and replace with `private var session: LanguageModelSessionImpl?`. Delete the `setAgentSession(_:)` method and replace with a `configureSession()` method that constructs LanguageModelSessionImpl with the current model provider, SQLiteMemoryStore, AgentPermissionBridge, and DefaultToolEngine. Register all tools from Batch1ToolRegistry, Batch23ToolRegistry, Batch45ToolRegistry via toolEngine.register(tool:metadata:).

    **Agent loop replacement in startAgentRun():**
    Delete `session.run(userInput:conversation:workingDirectory:onEvent:)` call (lines 360-369) and its StreamingQueryEvent callback. Replace with:
    ```
    let stream = await session.streamResponse(to: trimmed)
    for try await event in stream {
        await MainActor.run {
            self.handleSessionEvent(event, assistantID: assistantID)
        }
    }
    ```
    The agent loop (re-prompt on tool calls) is handled internally by LanguageModelSessionImpl — ThreadViewModel just iterates SessionEvent values. Per-turn persistence is automatic via SQLiteMemoryStore inside the session.

    **Replace handleStreamEvent() with handleSessionEvent():**
    Create `handleSessionEvent(_ event: SessionEvent, assistantID: String)`. Map all 6 cases:
    - `.textDelta(let text)`: `messages[index].appendText(text)` + persist on counter modulo.
    - `.thinkingDelta(let text)`: `messages[index].appendThinking(text)` + persist.
    - `.toolCallRequested(let id, let name, let input)`: Create `ToolUseBlock(toolUseID: id, toolName: name, inputSummary: input.truncated(...), inputDetail: input, status: .executing)`, add to message, persist.
    - `.toolCallCompleted(let id, let output, let isError)`: Update tool use block status to `.completed` or `.error(output)`, add `ToolResultBlock(toolUseID: id, content: output, isError: isError)`, persist.
    - `.turnCompleted(let usage, let stopReason)`: Persist intermediate state (handles multi-turn runs surviving crash), update token usage tracking.
    - `.error(let err)`: Add error block to message, set state to failed.

    **Remove conversation building:**
    Delete `buildConversation()` — session manages conversation via SQLiteMemoryStore. Remove `conversationHistory` parameter from any remaining references.

    **Remove AgentSessionManager dependencies:**
    Delete `agentSession?.cancelRun()` in cancel() — replace with streamingTask cancellation only. Delete `guard let session = agentSession, session.isBootstrapped` — replace with `guard let session = session else { return }`.

    **Update ThreadViewModel init / configuration:**
    The existing `setAgentSession(_:)` public method is replaced by `configure(modelProvider: any LanguageModel, workingDirectory: String)` which constructs LanguageModelSessionImpl. Callers (app startup flow) need updating separately or via the existing init path — preserve the existing init signature where possible and add modelProvider as a stored property.

    **Keep existing infrastructure:**
    Preserve: `messages: [AgentMessage]`, `state: ThreadState`, `streamingTask`, `messageQueue`, `cancel()`, `processNextQueued()`, `persistMessageWithBlocks()`, `persistState()`, `store: SwiftAgentStore?` (for existing metadata persistence until SQLiteMemoryStore fully replaces it). Do NOT delete SwiftAgentStore references yet — that happens in Task 2.
  </action>
  <acceptance_criteria>
    - Source: `grep -c 'LanguageModelSessionImpl' Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift` >= 1
    - Source: `grep -c 'streamResponse(to:' Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift` >= 1
    - Source: `grep -c 'AgentSessionManager' Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift` = 0
    - Source: `grep -c 'StreamingQueryEvent' Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift` = 0
    - Source: `grep -c 'handleSessionEvent' Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift` >= 1
    - Build: App target compiles after Task 2 deletions complete (this task alone won't compile because AppAgentProvider/AgentSessionManager references still exist in other files)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'LanguageModelSessionImpl' Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift</automated>
  </verify>
  <done>ThreadViewModel uses LanguageModelSessionImpl.streamResponse(to:). SessionEvent cases bridged to AgentMessage. AgentSessionManager no longer referenced.</done>
</task>

<task type="auto">
  <name>Task 2: Delete App/Agent/, App/LLM/, App/DeepSeek/, Storage/ (8 files), MessageNormalizer, and EvalCommand per D-12/D-13/D-14/D-15/D-16/D-17</name>
  <files>
    Sources/SwiftAgentApp/Agent/AgentSessionManager.swift
    Sources/SwiftAgentApp/Agent/AppAgentProvider.swift
    Sources/SwiftAgentApp/Agent/AgentMessages.swift
    Sources/SwiftAgentApp/LLM/AnthropicProvider.swift
    Sources/SwiftAgentApp/LLM/AppLLMProvider.swift
    Sources/SwiftAgentApp/LLM/DeepSeekProvider.swift
    Sources/SwiftAgentApp/LLM/LLMProvider.swift
    Sources/SwiftAgentApp/LLM/OpenAIProvider.swift
    Sources/SwiftAgentApp/LLM/ProviderRegistry.swift
    Sources/SwiftAgentApp/DeepSeek/DeepSeekClient.swift
    Sources/SwiftAgentApp/DeepSeek/DeepSeekConfig.swift
    Sources/SwiftAgentApp/DeepSeek/DeepSeekModel.swift
    Sources/SwiftAgentApp/DeepSeek/KeychainStore.swift
    Sources/SwiftAgentCore/Storage/SwiftAgentStore.swift
    Sources/SwiftAgentCore/Storage/SessionStore.swift
    Sources/SwiftAgentCore/Storage/MemoryStore.swift
    Sources/SwiftAgentCore/Storage/SessionIndexStore.swift
    Sources/SwiftAgentCore/Storage/ProjectDiscoverer.swift
    Sources/SwiftAgentCore/Storage/SessionDebugLog.swift
    Sources/SwiftAgentCore/Storage/SwiftAgentPaths.swift
    Sources/SwiftAgentCore/Storage/TranscriptStore.swift
    Sources/SwiftAgentCore/Agent/MessageNormalizer.swift
    Sources/SwiftAgentCore/Commands/EvalCommand.swift
  </files>
  <read_first>
    Sources/SwiftAgentApp/Agent/AgentSessionManager.swift — offset 1, limit 40
    Purpose: Verify references to types already deleted in 04-05 (LLMClient, QueryEngine, StreamEvent) — safety check.

    Sources/SwiftAgentApp/Agent/AppAgentProvider.swift — offset 1, limit 30
    Purpose: Verify no survivors reference AppAgentProvider after ThreadViewModel rewiring.

    Sources/SwiftAgentCore/Storage/SwiftAgentPaths.swift — offset 1, limit 20
    Purpose: Confirm nonisolated(unsafe) annotation — removing this file reduces the count from 4 to 3 (Plan 04-05 reduced to 3 by deleting LLMClient, this reduces to 2 by deleting SwiftAgentPaths).

    Sources/SwiftAgentCore/Agent/MessageNormalizer.swift — offset 1, limit 40
    Purpose: Verify MessageNormalizer references (ContentBlock, Message — types that may survive or be deleted elsewhere). Ensure no non-deleted file imports it.

    Sources/SwiftAgentCore/Commands/EvalCommand.swift — offset 1, limit 40
    Purpose: Verify EvalCommand references LLMClient, QueryEngine, ToolUseContext — all deleted in 04-05. Must be deleted too.
  </read_first>
  <action>
    Delete 24 deprecated files per D-12, D-13, D-14, D-15, D-16, D-17. This completes the deprecated type removal started in 04-05 (which deleted 13 Core files). For each group, safety-grep to verify no non-deleted files reference the type being deleted.

    **Group A — App/Agent/ (3 files):**
    1. AgentSessionManager.swift — Replaced by LanguageModelSessionImpl. Safety-grep: `grep -rn 'AgentSessionManager\b' Sources/SwiftAgentApp/ --include='*.swift' | grep -v 'AgentSessionManager.swift'`. Should return empty (ThreadViewModel was rewired in Task 1).
    2. AppAgentProvider.swift — Replaced by direct AnthropicProvider/DeepSeekProvider construction from AgentRuntime/Providers/.
    3. AgentMessages.swift — AgentMessage type definitions. See `<open_questions_resolved>` item 1: extract AgentMessage into `Sources/SwiftAgentApp/Types/AgentMessage.swift` if AgentMessages.swift is the sole definition; skip extraction if AgentMessage is defined elsewhere.

    **Group B — App/LLM/ (5 files):**
    4. AnthropicProvider.swift — Old App-side Anthropic provider wrapper. Replaced by AgentRuntime/Providers/AnthropicProvider.swift.
    5. AppLLMProvider.swift — App-side LLM provider abstraction. Replaced by LanguageModel protocol from AgentRuntime.
    6. DeepSeekProvider.swift — Old App-side DeepSeek provider. Replaced by AgentRuntime/Providers/DeepSeekProvider.swift.
    7. LLMProvider.swift — Enum/provider protocol. Replaced by LanguageModelExecutor.
    8. ProviderRegistry.swift — Provider registry (one of two nonisolated(unsafe) targets). Safety-grep before deletion.

    **Group C — App/DeepSeek/ (4 files):**
    9. DeepSeekClient.swift — Standalone DeepSeek API client. Replaced by AgentRuntime/Providers/DeepSeekProvider.
    10. DeepSeekConfig.swift — DeepSeek configuration types.
    11. DeepSeekModel.swift — Model enum (one of two nonisolated(unsafe) targets).
    12. KeychainStore.swift — API key management. See `<open_questions_resolved>` item 2: extract Keychain type to shared location if providers reference it; delete directly otherwise.

    **Group D — Storage/ (8 files) per D-14:**
    13. SwiftAgentStore.swift — Central file-based persistence.
    14. SessionStore.swift — Session metadata persistence.
    15. MemoryStore.swift — File-based memory storage (not to be confused with SessionMemoryStore protocol).
    16. SessionIndexStore.swift — Session index for sidebar.
    17. ProjectDiscoverer.swift — Project discovery from filesystem.
    18. SessionDebugLog.swift — Debug logging.
    19. SwiftAgentPaths.swift — Path utilities with nonisolated(unsafe) configHomeOverride. See `<open_questions_resolved>` item 3: migrate surviving references to SQLiteMemoryStore path configuration before deletion.
    20. TranscriptStore.swift — File-based transcript storage.

    **Group E — MessageNormalizer per D-15/D-16/D-17:**
    21. MessageNormalizer.swift (848 lines) — 17-pass shared normalization pipeline. See `<open_questions_resolved>` item 4: verify each provider's respond() covers essential passes, add missing passes to specific providers, then delete MessageNormalizer.swift. Do NOT create a shared TranscriptNormalizer.

    **Group F — EvalCommand:**
    22. EvalCommand.swift — Depends on LLMClient, QueryEngine, ToolUseContext (all deleted in 04-05). No survivors possible. Delete directly.

    **Post-deletion verification:**
    - `grep -rn 'nonisolated(unsafe)' Sources/ --include='*.swift'` should return only the 2 TerminalView closures (lines 117-118) — count should be 2 (down from 4 originally).
    - `grep -rn 'SwiftAgentStore\|SessionStore\|MemoryStore\b' Sources/SwiftAgentApp/ --include='*.swift'` — check for survivors in App code referencing deleted storage.
    - `grep -rn 'DeepSeekClient\|ProviderRegistry\|DeepSeekModel\b' Sources/ --include='*.swift'` — zero survivors.
    - Confirm the App target has no references to deleted LLM/ providers: `grep -rn 'AppLLMProvider\|LLMProvider\b' Sources/SwiftAgentApp/ --include='*.swift' | grep -v 'LLMProvider.swift'`.
  </action>
  <acceptance_criteria>
    - All 24 files: `ls <path>` returns "No such file" for each
    - nonisolated(unsafe) count = 2: `grep -rn 'nonisolated(unsafe)' Sources/ --include='*.swift' | wc -l` returns 2
    - Zero survivors referencing deleted types: `grep -rn 'AgentSessionManager\|AppAgentProvider\|DeepSeekClient\|ProviderRegistry\|SwiftAgentStore\.\|MessageNormalizer' Sources/SwiftAgentApp/ --include='*.swift'` returns empty
    - App target compiles after Task 1 rewiring: `swift build --disable-sandbox --target SwiftAgentApp` compiles (or if Xcode project, check via swift build for the package)
    - Core target compiles: `swift build --disable-sandbox --target SwiftAgentCore`
    - CLI target compiles: `swift build --disable-sandbox --target SwiftAgentCLI`
  </acceptance_criteria>
  <verify>
    <automated>ls Sources/SwiftAgentCore/Storage/SwiftAgentStore.swift 2>&1; ls Sources/SwiftAgentCore/Agent/MessageNormalizer.swift 2>&1; ls Sources/SwiftAgentApp/Agent/AgentSessionManager.swift 2>&1; ls Sources/SwiftAgentApp/DeepSeek/DeepSeekClient.swift 2>&1; ls Sources/SwiftAgentCore/Commands/EvalCommand.swift 2>&1</automated>
  </verify>
  <done>24 deprecated files deleted. App/Agent/ (3), App/LLM/ (5), App/DeepSeek/ (4), Storage/ (8), MessageNormalizer, EvalCommand all removed. nonisolated(unsafe) count: 2. Every deleted type has zero survivors in non-deleted files.</done>
</task>

<task type="auto">
  <name>Task 3: Fix Core agent/LLM/stream test files referencing deleted types</name>
  <files>
    Tests/SwiftAgentCoreTests/Phase1TypesTests.swift
    Tests/SwiftAgentCoreTests/Phase2LLMTests.swift
    Tests/SwiftAgentCoreTests/Phase3AgentTests.swift
    Tests/SwiftAgentCoreTests/AgentRuntimeImplTests.swift
    Tests/SwiftAgentCoreTests/AgentPermissionBridgeTests.swift
    Tests/SwiftAgentCoreTests/AnthropicProviderTests.swift
    Tests/SwiftAgentCoreTests/DeepSeekProviderTests.swift
    Tests/SwiftAgentCoreTests/OpenAIProviderTests.swift
    Tests/SwiftAgentCLITests/TerminalRenderingTests.swift
  </files>
  <read_first>
    Tests/SwiftAgentCoreTests/Phase1TypesTests.swift — offset 1, limit 30
    Purpose: Check Tool protocol, ToolUseContext, StreamEvent references. Old Tool deleted — tests need updating.

    Tests/SwiftAgentCoreTests/Phase2LLMTests.swift — offset 1, limit 30
    Purpose: Check LLMStreamParser, StreamEvent references. Both deleted — tests need rewriting against provider-internal types or deletion.

    Tests/SwiftAgentCoreTests/Phase3AgentTests.swift — offset 1, limit 30
    Purpose: Check QueryEngine, ContextManager, Message references. QueryEngine deleted in 04-05 — these tests need rewriting or deletion.

    Tests/SwiftAgentCLITests/TerminalRenderingTests.swift — offset 1, limit 30
    Purpose: Check StreamRenderer, StatusLine references. Both deleted in 04-05 — tests must reference SessionEventRenderer instead.
  </read_first>
  <action>
    Fix test files in the Core/CLI domain — those referencing agent loop types, LLM types, and stream types deleted in 04-05. This is subtask (a) of the original single task, split per checker Blocker 5.

    **Phase1TypesTests.swift:**
    - Tests referencing old Tool protocol, ToolUseContext → delete or rewrite against Tool protocol (renamed from RuntimeAgentTool in 04-04).
    - Tests referencing StreamEvent (old Anthropic-specific enum) → delete. SessionEvent is tested in AgentRuntimeImplTests.
    - Tests referencing JSONSchema/JSONSchemaProperty/JSONSchemaItems → keep (these were extracted, not deleted). Verify imports resolve.
    - Tests referencing InterruptBehavior → keep (extracted to standalone file).

    **Phase2LLMTests.swift:**
    - Tests of LLMStreamParser → delete. SSE parsing is now provider-internal.
    - Tests of StreamEvent parsing → delete. Covered by AnthropicProviderTests/DeepSeekProviderTests.
    - Tests of LLMClient → delete. Covered by AnthropicProviderTests.
    - Tests of ModelRegistry / ModelInfo → check if these types were deleted. If so, delete tests.

    **Phase3AgentTests.swift:**
    - Tests of QueryEngine.turn() → delete. Replaced by LanguageModelSessionImpl.
    - Tests of ContextManager → check if ContextManager is still needed (used by new AgentRuntime?). If deleted, delete tests.
    - Tests of ToolExecutor → delete. Replaced by ToolEngine.execute().
    - Tests of Compactor → delete (Compactor deleted in 04-05).
    - Tests of SubAgentManager → delete (SubAgentManager deleted in 04-05).

    **AgentRuntimeImplTests.swift, AgentPermissionBridgeTests.swift, AnthropicProviderTests.swift, DeepSeekProviderTests.swift, OpenAIProviderTests.swift:**
    - These were built in Phase 1-3 and should use new types. Check for any old type references (old Tool protocol, ToolUseContext, StreamEvent) — fix if found.

    **TerminalRenderingTests.swift:**
    - Tests referencing StreamRenderer, StatusLine → update to reference SessionEventRenderer.
    - Tests of rendering output format → verify the assertions match the new renderer's output.
  </action>
  <acceptance_criteria>
    - All 9 test files compile without errors referencing deleted types
    - Zero references to deleted types: `grep -rn 'LLMClient\|QueryEngine\|StreamEvent\b\|ToolExecutor\|SubAgentManager\|LLMStreamParser\|StreamRenderer\|StatusLine' Tests/SwiftAgentCoreTests/Phase1TypesTests.swift Tests/SwiftAgentCoreTests/Phase2LLMTests.swift Tests/SwiftAgentCoreTests/Phase3AgentTests.swift Tests/SwiftAgentCLITests/TerminalRenderingTests.swift 2>/dev/null | grep -v '//\|SKIP'` returns empty or only comment references
    - Build: `swift build --disable-sandbox --target SwiftAgentCore` compiles
  </acceptance_criteria>
  <verify>
    <automated>swift test --disable-sandbox --no-parallel --filter Phase1TypesTests 2>&1 | tail -5</automated>
  </verify>
  <done>9 Core/CLI test files updated. Deleted tests referencing deleted types kept (or rewritten). New-runtime tests verified clean.</done>
</task>

<task type="auto">
  <name>Task 4: Fix storage/App/tools test files referencing deleted types</name>
  <files>
    Tests/SwiftAgentCoreTests/Phase4ToolsTests.swift
    Tests/SwiftAgentCoreTests/Phase9StorageTests.swift
    Tests/SwiftAgentCoreTests/Phase11SubAgentTests.swift
    Tests/SwiftAgentCoreTests/SQLiteMemoryStoreTests.swift
    Tests/SwiftAgentAppTests/PersistenceRoundTripTests.swift
  </files>
  <read_first>
    Tests/SwiftAgentCoreTests/Phase4ToolsTests.swift — offset 1, limit 20
    Purpose: Check `testCtx = ToolUseContext(...)` usage at top level. ToolUseContext deleted — this must be rewritten to use per-tool let properties.

    Tests/SwiftAgentCoreTests/Phase9StorageTests.swift — offset 1, limit 30
    Purpose: Check SwiftAgentStore, SessionStore, MemoryStore references. These files are all deleted — tests need rewriting against SQLiteMemoryStore or deletion.

    Tests/SwiftAgentCoreTests/Phase11SubAgentTests.swift — offset 1, limit 30
    Purpose: Check AgentTool with old protocol, SubAgentManager references.
  </read_first>
  <action>
    Fix test files in the storage/App/tools domain — those referencing types deleted in Tasks 1-2 of this plan (ToolUseContext, SwiftAgentStore, SessionStore, AgentTool with old protocol). This is subtask (b) of the original single task, split per checker Blocker 5.

    **Phase4ToolsTests.swift:**
    - Critical: `let testCtx = ToolUseContext(...)` at file scope. All 60+ tool tests use testCtx. Replace with per-tool context: each test constructs let properties (workingDirectory, shell, etc.) and passes them to the migrated tool's init.
    - Tool tests calling `tool.call(input, context: testCtx)` with old `call(_:context:)` signature → rewrite to `tool.call(arguments: input)` per D-07.
    - Tests of individual tools that were migrated in 04-02/03/04 → update to new protocol calls.
    - Tests of tools NOT migrated → delete (old Tool protocol deleted).

    **Phase9StorageTests.swift:**
    - Tests of SwiftAgentStore, SessionStore, MemoryStore → delete or rewrite against SQLiteMemoryStore.
    - Tests of file-based persistence → check if SQLiteMemoryStoreTests covers equivalent cases. If yes, delete old tests.

    **Phase11SubAgentTests.swift:**
    - Tests of AgentTool with old protocol → update to migrated AgentTool under Tool protocol.
    - Tests of SubAgentManager → delete (deleted in 04-05).

    **SQLiteMemoryStoreTests.swift:**
    - Check for any old type references — fix if found. These should be clean (built in Phase 1-3).

    **PersistenceRoundTripTests.swift:**
    - Tests of file-based SwiftAgentStore persistence → delete (replaced by SQLiteMemoryStore).
  </action>
  <acceptance_criteria>
    - Phase4ToolsTests: `grep -c 'ToolUseContext' Tests/SwiftAgentCoreTests/Phase4ToolsTests.swift` returns 0
    - Phase4ToolsTests: `grep -c 'call(arguments:' Tests/SwiftAgentCoreTests/Phase4ToolsTests.swift` >= 10
    - Phase9StorageTests: `grep -c 'SwiftAgentStore\|SessionStore\|MemoryStore\b' Tests/SwiftAgentCoreTests/Phase9StorageTests.swift` returns 0
    - Phase11SubAgentTests: `grep -c 'SubAgentManager' Tests/SwiftAgentCoreTests/Phase11SubAgentTests.swift` returns 0
    - PersistenceRoundTripTests: either deleted or rewritten with zero SwiftAgentStore references
    - Build: `swift build --disable-sandbox` — all targets compile
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'ToolUseContext' Tests/SwiftAgentCoreTests/Phase4ToolsTests.swift</automated>
  </verify>
  <done>5 storage/App/tools test files updated. Zero ToolUseContext references. Legacy persistence tests deleted or rewritten.</done>
</task>

<task type="auto">
  <name>Task 5: Final sweep — MockProviders + verify full test suite passes per MIG-04</name>
  <files>
    Tests/SwiftAgentCoreTests/MockProviders.swift
  </files>
  <read_first>
    Tests/SwiftAgentCoreTests/MockProviders.swift — offset 1, limit 40
    Purpose: Check mock tool registrations — do they use old Tool protocol or RuntimeAgentTool? Update to use Tool protocol (renamed in 04-04).

    Run full test suite to identify any remaining failures:
    `swift test --disable-sandbox --no-parallel 2>&1 | grep "error:\|failed\|FAIL"` to find remaining issues.
  </read_first>
  <action>
    Final sweep of remaining test issues and MockProviders update. This is subtask (c) of the original single task, split per checker Blocker 5.

    **MockProviders.swift:**
    - Update mock tools to conform to Tool protocol (renamed from RuntimeAgentTool in 04-04) with Arguments associatedtype and call(arguments:) method.
    - Update mock registrations to use DefaultToolEngine.register(tool:metadata:).
    - Replace any `any RuntimeAgentTool` references with `any Tool`.

    **Final verification loop:**
    1. Run `swift build --disable-sandbox` — all targets compile with zero errors.
    2. Run `swift test --disable-sandbox --no-parallel` — all test suites pass.
    3. If specific test suites fail, fix them iteratively.
    4. If a test suite is entirely obsolete (tests only deleted types), delete the test file.
    5. Final check: `grep -rn 'LLMClient\|QueryEngine\|StreamEvent\b\|ToolExecutor\|SubAgentManager\|Compactor\|ToolUseContext\|AgentSessionManager\|DeepSeekClient\|SwiftAgentStore\|MessageNormalizer\|LLMStreamParser\|ToolRegistry' Tests/ --include='*.swift'` returns empty (or only in SKIP comments).
    6. Final nonisolated(unsafe) check: `grep -rn 'nonisolated(unsafe)' Sources/ Tests/ --include='*.swift' | wc -l` <= 2.
  </action>
  <acceptance_criteria>
    - MockProviders uses Tool protocol: `grep -c ': Tool\b\|any Tool\b' Tests/SwiftAgentCoreTests/MockProviders.swift` >= 1
    - MockProviders uses call(arguments:): `grep -c 'func call(arguments:' Tests/SwiftAgentCoreTests/MockProviders.swift` >= 1
    - Build: `swift build --disable-sandbox` — all targets (Core, CLI, App) compile with zero errors
    - Tests: `swift test --disable-sandbox --no-parallel` — all test suites pass (zero failures)
    - Zero references: `grep -rn 'LLMClient\|QueryEngine\|StreamEvent\b\|ToolExecutor\|SubAgentManager\|Compactor\|ToolUseContext\|AgentSessionManager\|DeepSeekClient\|SwiftAgentStore\|MessageNormalizer\|LLMStreamParser\|ToolRegistry' Tests/ --include='*.swift'` returns empty (or only in comments/docstrings)
    - nonisolated(unsafe) count: `grep -rn 'nonisolated(unsafe)' Sources/ Tests/ --include='*.swift' | wc -l` <= 2 (only TerminalView closures)
  </acceptance_criteria>
  <verify>
    <automated>swift test --disable-sandbox --no-parallel 2>&1 | tail -20</automated>
  </verify>
  <done>MockProviders updated to Tool protocol. Full test suite compiles and passes with zero failures. MIG-04 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App ViewModel -> session actor | ThreadViewModel crosses actor boundary for streamResponse(to:) |
| SessionEvent -> AgentMessage bridge | Untrusted model output rendered in NSTableView — no ANSI risk in App (NSTextView handles sanitization) |
| Old Storage/ deletion | Ensure no data loss — existing session data in ~/.swift-agent/ persists |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | SessionEvent.toolCallCompleted output in NSTableView | accept | NSTableView renders plain text, no ANSI injection surface |
| T-04-15 | DoS | ThreadViewModel reentrancy | mitigate | isResponding guard returns rateLimited error; messageQueue gates subsequent sends |
| T-04-16 | Information Disclosure | KeychainStore deletion | mitigate | Resolved by Task 2 pre-deletion check — extract Keychain type to shared location if providers reference it |
| T-04-SC | Tampering | npm/pip/cargo | accept | No external packages |
</threat_model>

<verification>
1. ThreadViewModel uses LanguageModelSessionImpl.streamResponse(to:) (no AgentSessionManager)
2. 24 deprecated files deleted (App/Agent/, App/LLM/, App/DeepSeek/, Storage/, MessageNormalizer, EvalCommand)
3. 15 test files updated across 3 focused tasks — zero references to deleted types
4. nonisolated(unsafe) count: 2
5. Full test suite passes: `swift test --disable-sandbox --no-parallel`
6. All targets compile: `swift build --disable-sandbox`
</verification>

<success_criteria>
App ThreadViewModel wired to LanguageModelSessionImpl (MIG-02 complete). All App-side deprecated types deleted (MIG-03 complete). Full test suite passes (MIG-04). MessageNormalizer deleted, per-provider normalization verified (MIG-05). Phase 04 migration complete.
</success_criteria>

<output>
Create `.planning/phases/04-migration-wiring-cleanup/04-06-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `ThreadViewModel.send()` | method (rewritten) | `Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift` | Uses LanguageModelSessionImpl.streamResponse(to:) |
| `ThreadViewModel.handleSessionEvent(_:assistantID:)` | method (new) | `Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift` | Bridges 6 SessionEvent cases to AgentMessage blocks |
| `ThreadViewModel.configureSession()` | method (new) | `Sources/SwiftAgentApp/ViewModels/ThreadViewModel.swift` | Constructs LanguageModelSessionImpl with providers, memory, tools |
| 24 deleted files | removal | Various | AgentSessionManager, AppAgentProvider, AgentMessages (+ extract AgentMessage), App/LLM/ (5), App/DeepSeek/ (4), Storage/ (8), MessageNormalizer, EvalCommand |
| 15 test files | update | Tests/ | Updated across 3 tasks to remove references to deleted types, rewritten where applicable |
| `AgentMessage` | type (extracted) | `App/Types/AgentMessage.swift` or similar | Extracted from AgentMessages.swift before that file is deleted |
