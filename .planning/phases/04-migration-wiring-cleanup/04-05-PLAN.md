---
phase: 04-migration-wiring-cleanup
plan: 05
type: execute
wave: 5
depends_on:
  - 04-04
files_modified:
  - Sources/SwiftAgentCLI/ChatCommand.swift
  - Sources/SwiftAgentCLI/ChatCommand+Types.swift
  - Sources/SwiftAgentCLI/ChatCommand+SystemPrompt.swift
  - Sources/SwiftAgentCLI/ChatCommand+ToolDisplay.swift
  - Sources/SwiftAgentCLI/ChatCommand+SessionPicker.swift
  - Sources/SwiftAgentCLI/ChatCommand+UserPrompt.swift
  - Sources/SwiftAgentCLI/SessionEventRenderer.swift
  - Sources/SwiftAgentCore/Agent/QueryEngine.swift
  - Sources/SwiftAgentCore/Agent/ToolExecutor.swift
  - Sources/SwiftAgentCore/Agent/StreamRenderer.swift
  - Sources/SwiftAgentCore/Agent/SubAgentManager.swift
  - Sources/SwiftAgentCore/Agent/Compactor.swift
  - Sources/SwiftAgentCore/LLM/LLMClient.swift
  - Sources/SwiftAgentCore/LLM/LLMStreamParser.swift
  - Sources/SwiftAgentCore/LLM/RetryPolicy.swift
  - Sources/SwiftAgentCore/LLM/SortedJSON.swift
  - Sources/SwiftAgentCore/Types/StreamEvent.swift
  - Sources/SwiftAgentCLI/ChatToolInputAccumulator.swift
  - Sources/SwiftAgentCLI/ChatToolExecutionScheduler.swift
  - Sources/SwiftAgentCLI/StatusLine.swift
autonomous: true
requirements:
  - MIG-02
  - MIG-03

must_haves:
  truths:
    - "ChatCommand.run() uses LanguageModelSessionImpl.streamResponse(to:) instead of manual agent loop"
    - "CLI rendering is fresh SessionEventRenderer, not adapted from StreamRenderer/StatusLine"
    - "LLMClient, QueryEngine, StreamEvent, and all old agent loop types are deleted"
  artifacts:
    - path: "Sources/SwiftAgentCLI/SessionEventRenderer.swift"
      provides: "Fresh CLI rendering for SessionEvent stream"
      min_lines: 80
    - path: "Sources/SwiftAgentCLI/ChatCommand.swift"
      provides: "Rewired ChatCommand using LanguageModelSessionImpl"
      contains: "LanguageModelSessionImpl"
  key_links:
    - from: "ChatCommand.run()"
      to: "LanguageModelSessionImpl.streamResponse(to:)"
      via: "AsyncThrowingStream<SessionEvent>"
      pattern: "streamResponse\\(to:"
    - from: "SessionEventRenderer.render(_:)"
      to: "SessionEvent cases"
      via: "switch on 6 event cases"
      pattern: "case \\.(textDelta|thinkingDelta|toolCallRequested|toolCallCompleted|turnCompleted|error)"

<objective>
Rewire ChatCommand to LanguageModelSessionImpl per D-08/D-09, build fresh SessionEventRenderer per D-10, and delete deprecated Core types per D-12. This is the CLI-facing half of consumer wiring.

Purpose: ChatCommand is the primary CLI entry point and currently uses the old agent loop (LLMClient + QueryEngine). Replacing it with LanguageModelSessionImpl removes the biggest consumer of deprecated types.
Output: ChatCommand wired to streamResponse(to:), SessionEventRenderer handling all 6 SessionEvent cases, 13 deprecated files deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-migration-wiring-cleanup/04-CONTEXT.md
@.planning/phases/04-migration-wiring-cleanup/04-RESEARCH.md

# Key source files
@Sources/SwiftAgentCLI/ChatCommand.swift
@Sources/SwiftAgentCore/AgentRuntime/AgentRuntimeImpl.swift
@Sources/SwiftAgentCore/AgentRuntime/SessionEvent.swift
@Sources/SwiftAgentCore/AgentRuntime/Tools/Batch1ToolRegistry.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire ChatCommand to LanguageModelSessionImpl per D-08/D-09</name>
  <files>
    Sources/SwiftAgentCLI/ChatCommand.swift
    Sources/SwiftAgentCLI/ChatCommand+Types.swift
    Sources/SwiftAgentCLI/ChatCommand+SystemPrompt.swift
    Sources/SwiftAgentCLI/ChatCommand+ToolDisplay.swift
    Sources/SwiftAgentCLI/ChatCommand+SessionPicker.swift
    Sources/SwiftAgentCLI/ChatCommand+UserPrompt.swift
  </files>
  <read_first>
    Sources/SwiftAgentCLI/ChatCommand.swift — offset 46, limit 120
    Purpose: Read run() initialization — LLMClient, ToolRegistry, QueryEngine construction.

    Sources/SwiftAgentCLI/ChatCommand.swift — offset 540, limit 300
    Purpose: Read the current agent loop — LLMClient.send(), StreamEvent iteration, ChatToolInputAccumulator, ChatToolExecutionScheduler, conversationHistory.

    Sources/SwiftAgentCore/AgentRuntime/AgentRuntimeImpl.swift
    Purpose: Confirm LanguageModelSessionImpl.init params and streamResponse(to:) signature.

    Sources/SwiftAgentCore/AgentRuntime/Tools/Batch1ToolRegistry.swift
    Purpose: Understand tool registration API shape.
  </read_first>
  <action>
    Replace ChatCommand's manual agent loop (~270 lines) with LanguageModelSessionImpl. This is a direct replacement per D-08 (no feature flags) and D-09 (CLI uses streamResponse).

    **Step 1 — Replace subsystem construction:**
    Delete: LLMClient(apiKey:baseURL:model:), ToolRegistry(), ToolExecutor(registry:), QueryEngine(client:toolExecutor:...), SubAgentManager, registerBuiltinTools calls, DynamicMCPTool registration.
    Replace with: DeepSeekProvider construction, SQLiteMemoryStore, AgentPermissionBridge, DefaultToolEngine. Construct LanguageModelSessionImpl with all subsystems. Register tools from Batch1ToolRegistry, Batch23ToolRegistry, Batch45ToolRegistry using toolEngine.register(tool:metadata:).

    **Step 2 — Replace the agent loop:**
    Delete: while-true loop, client.send(), for-await StreamEvent block, ChatToolInputAccumulator parsing, ChatToolExecutionScheduler.execute(), manual executeTool(), conversationHistory.append(), emitCollapsedResults().
    Replace: `let stream = await session.streamResponse(to: userInput)` followed by `for try await event in stream { renderer.render(event) }`.

    **Step 3 — Remove conversationHistory:** Delete `var conversationHistory: [Message]` and all append/load operations. Session resume from file-based JSONL is deferred — SQLiteMemoryStore persists after each turn.

    **Step 4 — Handle MCP bootstrapping:** Register MCP tools into DefaultToolEngine instead of old ToolRegistry.

    **Step 5 — Handle /model switching:** Preserve SharedModel pattern. Provider reads sharedModel.current.

    **Step 6 — Update extension files:** Remove types only used by old agent loop from ChatCommand+Types.swift. Migrate SystemPromptBuilder to AgentProfile.instructions if feasible.
  </action>
  <acceptance_criteria>
    - Source: `grep -c 'LanguageModelSessionImpl(' Sources/SwiftAgentCLI/ChatCommand.swift` >= 1
    - Source: `grep -c 'streamResponse(to:' Sources/SwiftAgentCLI/ChatCommand.swift` >= 1
    - Source: `grep -c 'LLMClient(\|ToolRegistry()\|QueryEngine(' Sources/SwiftAgentCLI/ChatCommand.swift` = 0
    - Source: `grep -c 'conversationHistory\|ChatToolInputAccumulator\|ChatToolExecutionScheduler' Sources/SwiftAgentCLI/ChatCommand.swift` = 0
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'streamResponse(to:' Sources/SwiftAgentCLI/ChatCommand.swift</automated>
  </verify>
  <done>ChatCommand uses LanguageModelSessionImpl.streamResponse(to:). Manual agent loop removed. Tools registered via batch registries.</done>
</task>

<task type="auto">
  <name>Task 2: Build new SessionEventRenderer for CLI per D-10</name>
  <files>
    Sources/SwiftAgentCLI/SessionEventRenderer.swift
  </files>
  <read_first>
    Sources/SwiftAgentCore/AgentRuntime/SessionEvent.swift
    Purpose: All 6 SessionEvent cases and associated values.

    Sources/SwiftAgentCLI/TerminalRenderer.swift — offset 1, limit 60
    Purpose: TerminalRenderer API for ANSI output, spinners, colors.

    Sources/SwiftAgentCLI/ColorTheme.swift — offset 1, limit 40
    Purpose: Color tokens for consistent styling.
  </read_first>
  <action>
    Build NEW SessionEventRenderer per D-10. Create `Sources/SwiftAgentCLI/SessionEventRenderer.swift`. A struct accepting TerminalRenderer, MarkdownRenderer, ColorTheme at init. Single `render(_ event: SessionEvent)` method handling all 6 cases:

    - `.textDelta(let text)`: Print accumulated snapshot to stdout. Use ANSI clear-to-end-of-line + carriage return for in-place updates, or print full snapshot. Normal color.
    - `.thinkingDelta(let text)`: Print dimmed thinking (ANSI `\u{001B}[2m`) when `showThinking: Bool` is true.
    - `.toolCallRequested(let id, let name, let input)`: Show spinner with bold tool name and dimmed input preview (first 100 chars). Track active tools by id.
    - `.toolCallCompleted(let id, let output, let isError)`: Clear spinner. Green check + result or red X + error.
    - `.turnCompleted(let usage, let stopReason)`: Print dimmed usage stats `[Tokens: in=X out=Y]`.
    - `.error(let err)`: Print error in red.

    Key rules: NO references to StreamRenderer, StatusLine, StreamEvent, ChatToolInputAccumulator. Fresh implementation. Handle snapshot semantics (replace previous output, never append deltas).
  </action>
  <acceptance_criteria>
    - Exist: `ls Sources/SwiftAgentCLI/SessionEventRenderer.swift` returns path
    - Fresh: `grep -c 'StreamRenderer\|StatusLine\|StreamEvent\b' Sources/SwiftAgentCLI/SessionEventRenderer.swift` = 0
    - All cases handled: grep shows 6 case labels for SessionEvent
    - Length: `wc -l < Sources/SwiftAgentCLI/SessionEventRenderer.swift` >= 80
  </acceptance_criteria>
  <verify>
    <automated>wc -l < Sources/SwiftAgentCLI/SessionEventRenderer.swift</automated>
  </verify>
  <done>SessionEventRenderer handles all 6 SessionEvent cases fresh. No old rendering code.</done>
</task>

<task type="auto">
  <name>Task 3: Delete deprecated Core types per D-12</name>
  <files>
    Sources/SwiftAgentCore/Agent/QueryEngine.swift
    Sources/SwiftAgentCore/Agent/ToolExecutor.swift
    Sources/SwiftAgentCore/Agent/StreamRenderer.swift
    Sources/SwiftAgentCore/Agent/SubAgentManager.swift
    Sources/SwiftAgentCore/Agent/Compactor.swift
    Sources/SwiftAgentCore/LLM/LLMClient.swift
    Sources/SwiftAgentCore/LLM/LLMStreamParser.swift
    Sources/SwiftAgentCore/LLM/RetryPolicy.swift
    Sources/SwiftAgentCore/LLM/SortedJSON.swift
    Sources/SwiftAgentCore/Types/StreamEvent.swift
    Sources/SwiftAgentCLI/ChatToolInputAccumulator.swift
    Sources/SwiftAgentCLI/ChatToolExecutionScheduler.swift
    Sources/SwiftAgentCLI/StatusLine.swift
  </files>
  <read_first>
    Sources/SwiftAgentCore/Agent/QueryEngine.swift — offset 1, limit 30
    Purpose: Verify dependencies before deletion.

    Sources/SwiftAgentCore/Types/StreamEvent.swift — offset 1, limit 50
    Purpose: Confirm no survivors reference old StreamEvent.
  </read_first>
  <action>
    Delete 13 deprecated files per D-12. For each file, safety-grep first to verify no non-deleted files reference the type being deleted.

    Files to delete (rm):
    1. QueryEngine.swift — replaced by LanguageModelSessionImpl
    2. ToolExecutor.swift — replaced by ToolEngine.execute()
    3. StreamRenderer.swift — replaced by SessionEventRenderer
    4. SubAgentManager.swift — replaced by AgentTool using LanguageModelSessionImpl directly
    5. Compactor.swift — deferred per RESEARCH.md recommendation
    6. LLMClient.swift — replaced by AgentRuntime/Providers/. Removes 1 nonisolated(unsafe) annotation.
    7. LLMStreamParser.swift — replaced by provider-internal SSE parsers
    8. RetryPolicy.swift — replaced by AnthropicRetryPolicy
    9. SortedJSON.swift — replaced by provider-internal JSON handling
    10. StreamEvent.swift — old Anthropic-specific enum, replaced by SessionEvent
    11. ChatToolInputAccumulator.swift — replaced by provider-internal SSE parsing
    12. ChatToolExecutionScheduler.swift — replaced by ToolEngine managing execution
    13. StatusLine.swift — replaced by SessionEventRenderer

    Safety check for each: `grep -rn 'TypeName\b' Sources/ --include='*.swift' | grep -v '/TypeName.swift'`. If survivors exist in non-deleted files, update them first.

    Post-deletion: `grep -rn 'nonisolated(unsafe)' Sources/ --include='*.swift' | wc -l` should be 3 (LLMClient.swift removed — was the 4th). Verify StreamEvent survivors: `grep -rn 'StreamEvent\b' Sources/ --include='*.swift' | grep -v 'SessionEvent\|StreamEvent.swift'` returns empty.
  </action>
  <acceptance_criteria>
    - All 13 files: `ls <path>` returns "No such file" for each
    - nonisolated(unsafe) count = 3: `grep -rn 'nonisolated(unsafe)' Sources/ --include='*.swift' | wc -l` returns 3
    - No StreamEvent survivors outside deleted files
    - CLI target compiles: `swift build --disable-sandbox --target SwiftAgentCLI` succeeds
  </acceptance_criteria>
  <verify>
    <automated>ls Sources/SwiftAgentCore/Agent/QueryEngine.swift 2>&1; ls Sources/SwiftAgentCore/LLM/LLMClient.swift 2>&1; ls Sources/SwiftAgentCore/Types/StreamEvent.swift 2>&1</automated>
  </verify>
  <done>13 deprecated files deleted. nonisolated(unsafe) count: 3 (decreased from 4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CLI → session actor | ChatCommand crosses actor boundary for streamResponse(to:) |
| SessionEvent → terminal | Tool output rendered to terminal — ANSI injection risk |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering | Tool output in SessionEventRenderer | mitigate | Strip/escape ANSI sequences in tool output before rendering |
| T-04-13 | DoS | Session actor reentrancy | mitigate | isResponding guard returns rateLimited error |
| T-04-SC | Tampering | npm/pip/cargo | accept | No external packages |
</threat_model>

<verification>
1. ChatCommand uses LanguageModelSessionImpl (no old agent loop types)
2. SessionEventRenderer handles all 6 SessionEvent cases fresh
3. 13 deprecated files deleted
4. nonisolated(unsafe) count: 3
5. CLI target compiles
</verification>

<success_criteria>
CLI ChatCommand wired to LanguageModelSessionImpl (MIG-02 partial). SessionEventRenderer built (D-10). 13 deprecated Core types deleted. CLI target compiles. Consumer wiring 50% complete — App wiring remains in Plan 04-06.
</success_criteria>

<output>
Create `.planning/phases/04-migration-wiring-cleanup/04-05-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `SessionEventRenderer` | struct | `Sources/SwiftAgentCLI/SessionEventRenderer.swift` | Fresh CLI rendering for all 6 SessionEvent cases |
| `ChatCommand.run()` | method (rewritten) | `Sources/SwiftAgentCLI/ChatCommand.swift` | Uses LanguageModelSessionImpl |
| 13 deleted files | removal | Various | QueryEngine, LLMClient, LLMStreamParser, StreamRenderer, StreamEvent, ToolExecutor, SubAgentManager, Compactor, ChatToolInputAccumulator, ChatToolExecutionScheduler, StatusLine, RetryPolicy, SortedJSON |
