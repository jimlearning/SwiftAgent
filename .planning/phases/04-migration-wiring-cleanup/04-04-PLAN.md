---
phase: 04-migration-wiring-cleanup
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/TaskCreateTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/TaskUpdateTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/TaskStopTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/TaskOutputTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/TodoWriteTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/TeamCreateTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/TeamDeleteTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/SendMessageTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/EnterPlanModeTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/ExitPlanModeV2Tool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/VerifyPlanExecutionTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/SyntheticOutputTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/WorkflowTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/REPLMode.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/EnterWorktreeTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/ExitWorktreeTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/SuggestBackgroundPRTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/SubscribePRTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/SleepTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/MCPTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/DynamicMCPTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/McpAuthTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/ConfigTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/TestingPermissionTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/SkillTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/AskUserQuestionTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/CronCreateTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/CronDeleteTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/CronListTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/PushNotificationTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/RemoteTriggerTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/MonitorTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/SendUserFileTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/WebBrowserTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/OverflowTestTool.swift
  - Sources/SwiftAgentCore/AgentRuntime/Tools/Batch45ToolRegistry.swift
  - Sources/SwiftAgentCore/Types/BuiltInAgents.swift
  - Sources/SwiftAgentCore/Types/CronTypes.swift
  - Sources/SwiftAgentCore/Types/WorktreeTypes.swift
  - Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift
  - Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift
  - Sources/SwiftAgentCore/AgentRuntime/AgentRuntimeImpl.swift
autonomous: true
requirements:
  - MIG-01
  - MIG-03

must_haves:
  truths:
    - "All 36 remaining tools compile as Tool conformers in AgentRuntime/Tools/"
    - "Old Tools/ directory is empty (all tool files deleted or relocated)"
    - "AgentTool creates child LanguageModelSessionImpl instances for sub-agent spawning"
    - "MCP tools capture client references at init time per D-03"
    - "Closure-based context (setPlanModeActive, userInputPromptHandler) captured as @Sendable closures at init"
  artifacts:
    - path: "Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift"
      provides: "Tool protocol renamed from RuntimeAgentTool, with typealias for backward compat"
      contains: "public protocol Tool<Arguments>"
    - path: "Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift"
      provides: "AgentTool — sub-agent spawning"
      min_lines: 80
    - path: "Sources/SwiftAgentCore/AgentRuntime/Tools/AskUserQuestionTool.swift"
      provides: "AskUserQuestionTool"
      min_lines: 50
    - path: "Sources/SwiftAgentCore/AgentRuntime/Tools/SkillTool.swift"
      provides: "SkillTool"
      min_lines: 60
  key_links:
    - from: "AgentTool.call(arguments:)"
      to: "LanguageModelSessionImpl (child session)"
      via: "self.modelProvider + self.memoryStore captured as let properties"
      pattern: "LanguageModelSessionImpl\\(modelProvider:"
    - from: "MCPTool.call(arguments:)"
      to: "MCP client reference"
      via: "captured at init"
      pattern: "self\\.mcpClients"
    - from: "EnterPlanModeTool.call(arguments:)"
      to: "setPlanModeActive closure"
      via: "captured @Sendable closure at init"
      pattern: "self\\.setPlanModeActive\\(true\\)"
---

<objective>
Migrate the final 36 tools to the Tool protocol (Batches 4+5), relocate 3 shared type files, delete all remaining old tool files from Sources/SwiftAgentCore/Tools/, rename RuntimeAgentTool to Tool, and create the Batch45ToolRegistry. After this plan, all 63 tools are migrated — MIG-01 is complete.

**Protocol rename moved to Task 1 start (per checker Blocker 3):** The rename from RuntimeAgentTool to Tool happens first, so all tools created in this plan use `: Tool` directly. This depends on 04-03 having already deleted the old `Tool` protocol from Types/Tool.swift, freeing the name.

Purpose: Complete the tool migration. Without this plan, 36 tools still reference the deleted old Tool protocol (compile errors). After this plan, no code references ToolUseContext or the old Tool protocol.
Output: 36 new tool files, Tools/ directory empty, RuntimeAgentTool renamed to Tool, 3 batch registries covering all 63 tools.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-migration-wiring-cleanup/04-CONTEXT.md
@.planning/phases/04-migration-wiring-cleanup/04-RESEARCH.md
@.planning/REQUIREMENTS.md

# Complex tool references
@Sources/SwiftAgentCore/Tools/AgentTool.swift
@Sources/SwiftAgentCore/Tools/SkillTool.swift
@Sources/SwiftAgentCore/Tools/AskUserQuestionTool.swift
@Sources/SwiftAgentCore/Tools/MCPTool.swift
@Sources/SwiftAgentCore/AgentRuntime/AgentRuntimeImpl.swift

# Pattern reference
@Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift
@Sources/SwiftAgentCore/AgentRuntime/Tools/Batch1ToolRegistry.swift

# ToolEngine protocol (needs update after rename)
@Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift

# DefaultToolEngine (needs update after rename)
@Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename RuntimeAgentTool to Tool per Claude's discretion, then migrate Batch 4 — 20 task/agent/workflow tools</name>
  <files>
    Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/TaskCreateTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/TaskUpdateTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/TaskStopTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/TaskOutputTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/TodoWriteTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/TeamCreateTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/TeamDeleteTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/SendMessageTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/EnterPlanModeTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/ExitPlanModeV2Tool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/VerifyPlanExecutionTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/SyntheticOutputTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/WorkflowTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/REPLMode.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/EnterWorktreeTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/ExitWorktreeTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/SuggestBackgroundPRTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/SubscribePRTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/SleepTool.swift
    Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift
    Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift
    Sources/SwiftAgentCore/AgentRuntime/AgentRuntimeImpl.swift
  </files>
  <read_first>
    Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift
    Purpose: Read current protocol definition to understand exact member names, doc comments, and the file structure before renaming.

    Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift
    Purpose: Confirm ToolEngine protocol references `any RuntimeAgentTool` — must update to `any Tool`.

    Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift
    Purpose: Confirm DefaultToolEngine references — must update to `any Tool` after rename.

    Sources/SwiftAgentCore/Tools/AgentTool.swift
    Purpose: Most complex tool (289 lines). Understand sub-agent spawning via SubAgentManager/QueryEngine, context fields (sessionID, workingDirectory, mode, mainLoopModel, toolUseID).

    Sources/SwiftAgentCore/Tools/EnterPlanModeTool.swift + Sources/SwiftAgentCore/Tools/ExitPlanModeV2Tool.swift
    Purpose: Closure-based context (setPlanModeActive) — pattern reference for @Sendable closure capture.

    Sources/SwiftAgentCore/AgentRuntime/AgentRuntimeImpl.swift
    Purpose: LanguageModelSessionImpl.init signature for AgentTool's child session construction.
  </read_first>
  <action>
    **PART 1 — Rename RuntimeAgentTool to Tool (moved to beginning per checker Blocker 3 fix):**

    Per CONTEXT.md Claude's Discretion: "Final tool protocol name after old protocol removal." Since 04-03 deleted the old `Tool` protocol from Types/Tool.swift, the name `Tool` is now free. Rename RuntimeAgentTool to Tool before creating any new tools, so all Batch 4 tools use `: Tool` directly.

    In RuntimeAgentTool.swift:
    1. Rename `public protocol RuntimeAgentTool<Arguments>` to `public protocol Tool<Arguments>`.
    2. Add typealias: `public typealias RuntimeAgentTool = Tool` for backward compat with Batch 1-3 tools that still reference `RuntimeAgentTool`.
    3. Update the doc comment: replace "Named RuntimeAgentTool with the Runtime prefix to coexist alongside the old Tool protocol" explanation with "Formerly RuntimeAgentTool — renamed to Tool after old Tool protocol deletion in Phase 4."
    4. **DO NOT rename the file.** Keep the filename `RuntimeAgentTool.swift` to avoid cascading import changes. The file contains the `Tool` protocol definition with a `RuntimeAgentTool` typealias — the filename is historical.

    Update ALL existing files that reference `RuntimeAgentTool` (these were created by 04-02 and 04-03):
    5. Grep `Sources/SwiftAgentCore/AgentRuntime/` for `RuntimeAgentTool` and update to `Tool`. This includes:
       - `AgentRuntime.swift`: ToolEngine protocol uses `any RuntimeAgentTool` → `any Tool`
       - `SubsystemStubs.swift`: DefaultToolEngine registry uses `any RuntimeAgentTool` → `any Tool`
       - `AgentRuntimeImpl.swift`: Any RuntimeAgentTool references → `Tool`
       - All Batch 1-3 tool files in `AgentRuntime/Tools/` that use `: RuntimeAgentTool` → `: Tool` (15 + 9 = 24 files)
    6. Update Batch1ToolRegistry and Batch23ToolRegistry return types from `any RuntimeAgentTool` to `any Tool`.

    **PART 2 — Migrate 20 Batch 4 tools:**

    Now that the protocol is named `Tool`, every new tool uses `: Tool` conformance directly. This batch includes AgentTool (sub-agent spawning — highest complexity) and closure-based tools (EnterPlanMode, ExitPlanModeV2).

    **Standard tools** (no special handling, same pattern as Batch 1):
    TaskCreateTool, TaskUpdateTool, TaskStopTool, TeamCreateTool, TeamDeleteTool, SendMessageTool, VerifyPlanExecutionTool, SyntheticOutputTool, WorkflowTool, REPLMode, SuggestBackgroundPRTool, SubscribePRTool, SleepTool, ExitWorktreeTool. No context fields needed. Typed Arguments from old inputSchema.

    **TodoWriteTool**: context.sessionID → `let sessionID: String` at init.

    **TaskOutputTool** — context.toolUseID:
    > RESOLVED: For TaskOutputTool, toolUseID is the tool's own identifier within the ToolUseContext for self-referential output routing. In the new architecture, the LanguageModelSessionImpl.executeTool() method passes the toolUseID to the tool execution context. The executor will determine the correct approach by reading the old implementation; if toolUseID is truly self-referential from the model's perspective, it comes from the tool-call event's block ID passed through SessionEvent, not captured at init.

    **EnterWorktreeTool**: context.workingDirectory → `let workingDirectory: String` at init.

    **EnterPlanModeTool / ExitPlanModeV2Tool**: context.setPlanModeActive → `let setPlanModeActive: @Sendable (Bool) -> Void` at init. The `@Sendable` is required for actor isolation safety in the new architecture.

    **AgentTool — SUB-AGENT SPAWNING (critical)**:

    The old AgentTool (289 lines) spawns sub-agents via SubAgentManager which creates QueryEngine instances. In the new architecture, per RESEARCH.md: "AgentTool creates a new LanguageModelSessionImpl with the same providers but a fresh transcript."

    Init properties (captured for child session construction):
    - `let modelProvider: any LanguageModel` — for `childSession.makeExecutor()`
    - `let memoryStore: any SessionMemoryStore` — for child session memory persistence
    - `let permissionEngine: any SessionPermissionEngine` — for child session permission checks
    - `let toolEngine: any ToolEngine` — for child session tool execution
    - `let workingDirectory: String` — for context-aware sub-agents
    - `let mainLoopModel: String` — the model identifier for the main loop

    In call(arguments:), AgentTool creates a fresh LanguageModelSessionImpl, registers appropriate tools, and calls childSession.respond(to:) or streamResponse(to:). This eliminates the dependency on SubAgentManager.

    For ALL tools: BEFORE writing the migration, grep the old file for `context\.` to verify context field usage (the RESEARCH.md analysis might miss private helper accesses). Capture EVERY accessed field at init.
  </action>
  <acceptance_criteria>
    - Protocol renamed: `grep -c 'public protocol Tool<Arguments>' Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift` returns 1
    - Typealias: `grep -c 'typealias RuntimeAgentTool = Tool' Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift` returns 1
    - ToolEngine updated: `grep -c 'any Tool\b' Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift` returns at least 1
    - SubsystemStubs updated: `grep -c 'any Tool\b' Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift` returns at least 1
    - Count: `ls Sources/SwiftAgentCore/AgentRuntime/Tools/{Agent,TaskCreate,TaskUpdate,TaskStop,TaskOutput,TodoWrite,TeamCreate,TeamDelete,SendMessage,EnterPlanMode,ExitPlanModeV2,VerifyPlanExecution,SyntheticOutput,Workflow,REPLMode,EnterWorktree,ExitWorktree,SuggestBackgroundPR,SubscribePR,Sleep}*.swift 2>/dev/null | wc -l` returns 20
    - Conformance: `grep -l ': Tool\b' Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift Sources/SwiftAgentCore/AgentRuntime/Tools/TaskCreateTool.swift | wc -l` returns 2
    - AgentTool sub-agent: `grep -c 'LanguageModelSessionImpl' Sources/SwiftAgentCore/AgentRuntime/Tools/AgentTool.swift` returns at least 1
    - Closure capture: `grep -c 'setPlanModeActive.*Bool.*Void' Sources/SwiftAgentCore/AgentRuntime/Tools/EnterPlanModeTool.swift` returns at least 1
    - No ToolUseContext in new files: `grep -l 'ToolUseContext' Sources/SwiftAgentCore/AgentRuntime/Tools/*.swift` returns empty
    - Build: `swift build --disable-sandbox --target SwiftAgentCore` compiles (the 16 Batch 5 old files still failing to compile is expected; Batch 4 files should compile)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'public protocol Tool<Arguments>' Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift</automated>
  </verify>
  <done>Protocol renamed to Tool with RuntimeAgentTool typealias. All existing files updated. 20 Batch 4 tools as Tool conformers. AgentTool spawns child LanguageModelSessionImpl instances. No ToolUseContext references in AgentRuntime/Tools/.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Batch 5 — 16 tools + relocate shared types + delete all remaining old tool files</name>
  <files>
    Sources/SwiftAgentCore/AgentRuntime/Tools/MCPTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/DynamicMCPTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/McpAuthTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/ConfigTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/TestingPermissionTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/SkillTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/AskUserQuestionTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/CronCreateTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/CronDeleteTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/CronListTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/PushNotificationTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/RemoteTriggerTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/MonitorTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/SendUserFileTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/WebBrowserTool.swift
    Sources/SwiftAgentCore/AgentRuntime/Tools/OverflowTestTool.swift
    Sources/SwiftAgentCore/Types/BuiltInAgents.swift
    Sources/SwiftAgentCore/Types/CronTypes.swift
    Sources/SwiftAgentCore/Types/WorktreeTypes.swift
  </files>
  <read_first>
    Sources/SwiftAgentCore/Tools/SkillTool.swift
    Purpose: Complex tool (221 lines) using commands context. Understand skill loading/execution flow.

    Sources/SwiftAgentCore/Tools/AskUserQuestionTool.swift
    Purpose: Interactive tool (159 lines) using userInputPromptHandler closure. Understand the prompt/response flow.

    Sources/SwiftAgentCore/Tools/MCPTool.swift + Sources/SwiftAgentCore/Tools/DynamicMCPTool.swift + Sources/SwiftAgentCore/Tools/McpAuthTool.swift
    Purpose: MCP server integration. Understand mcpClients context usage.

    Sources/SwiftAgentCore/Tools/BuiltInAgents.swift
    Purpose: Static agent definitions (232 lines). Determine migration vs relocation.
  </read_first>
  <action>
    Final tool migration batch — 16 tools plus relocation and deletion (split from original Task 2 per checker Blocker 4). Protocol rename already done in Task 1, so all new tools use `: Tool` directly.

    PART A — Migrate 16 Batch 5 tools:

    **Standard tools** (same pattern): TestingPermissionTool, ConfigTool, CronCreateTool, CronDeleteTool, CronListTool, PushNotificationTool, RemoteTriggerTool, MonitorTool, SendUserFileTool, WebBrowserTool, OverflowTestTool.

    **MCPTool, DynamicMCPTool, McpAuthTool** — context.mcpClients:
    - Init: `let mcpClients: [any Sendable]` (or actual MCP client type)
    - DynamicMCPTool also captures `serverName: String`, `toolName: String` at init (MCP bootstrapper provides these)
    - McpAuthTool also captures mcpResources reference
    - call(arguments:): Route tool execution through the captured MCP client

    **SkillTool** — context.commands:
    - Init: `let commands: [any Sendable]?`
    - call(arguments:): Skill file loading, parsing, and execution

    **AskUserQuestionTool** — context.userInputPromptHandler:
    - Init: `let userInputPromptHandler: @Sendable (String) async -> String?`
    - call(arguments:): Present question via closure, await user response, return as ToolOutputValue

    PART B — Relocate 3 shared type files:
    Move from Sources/SwiftAgentCore/Tools/ to Sources/SwiftAgentCore/Types/:
    - BuiltInAgents.swift (static agent definitions — 232 lines)
    - CronTypes.swift (shared Cron types — 90 lines)
    - WorktreeTypes.swift (shared Worktree types — 46 lines)

    These are NOT tools — they define shared types. Moving to Types/ matches the convention where domain types live. No import changes needed (same module).

    PART C — Delete ALL remaining old tool files from Sources/SwiftAgentCore/Tools/:
    Delete all 36 old Batch 4+5 tool files. After this, `ls Sources/SwiftAgentCore/Tools/` should return empty (shared types already moved). If any straggler files remain, identify and handle them.
  </action>
  <acceptance_criteria>
    - Batch 5 count: `grep -l ': Tool\b' Sources/SwiftAgentCore/AgentRuntime/Tools/MCPTool.swift Sources/SwiftAgentCore/AgentRuntime/Tools/SkillTool.swift Sources/SwiftAgentCore/AgentRuntime/Tools/AskUserQuestionTool.swift | wc -l` returns 3
    - Tools/ empty: `ls Sources/SwiftAgentCore/Tools/*.swift 2>/dev/null | wc -l` returns 0
    - Shared types relocated: `ls Sources/SwiftAgentCore/Types/{BuiltInAgents,CronTypes,WorktreeTypes}.swift 2>/dev/null | wc -l` returns 3
    - AskUserQuestion closure: `grep -c '@Sendable.*String.*async.*String' Sources/SwiftAgentCore/AgentRuntime/Tools/AskUserQuestionTool.swift` returns at least 1
    - No ToolUseContext in new files: `grep -l 'ToolUseContext' Sources/SwiftAgentCore/AgentRuntime/Tools/*.swift` returns empty
    - Build: `swift build --disable-sandbox --target SwiftAgentCore` compiles with zero errors related to missing tool files (there may be other errors from unmigrated consumers — ChatCommand, etc. — which are addressed in Plans 04-05/04-06)
  </acceptance_criteria>
  <verify>
    <automated>ls Sources/SwiftAgentCore/Tools/*.swift 2>/dev/null | wc -l</automated>
  </verify>
  <done>All 16 Batch 5 tools migrated to Tool protocol. Tools/ directory empty. 3 shared type files relocated to Types/. All 36 old tool files deleted. Batch 5 migration complete (excluding registry, which is Task 3).</done>
</task>

<task type="auto">
  <name>Task 3: Create Batch45ToolRegistry for 36 Batch 4+5 tools</name>
  <files>
    Sources/SwiftAgentCore/AgentRuntime/Tools/Batch45ToolRegistry.swift
  </files>
  <read_first>
    Sources/SwiftAgentCore/AgentRuntime/Tools/Batch1ToolRegistry.swift
    Purpose: Pattern reference for creating Batch45ToolRegistry with matching structure.

    Sources/SwiftAgentCore/AgentRuntime/Tools/ToolMetadata.swift
    Purpose: Confirm ToolMetadata struct fields and init signature.
  </read_first>
  <action>
    Create the final tool registry covering all 36 Batch 4+5 tools. Each entry pairs a tool with its ToolMetadata. Follow the Batch1ToolRegistry pattern established in 04-02.

    Registry structure:
    - Define `Batch45ToolRegistry.tools(modelProvider:memoryStore:permissionEngine:toolEngine:workingDirectory:mainLoopModel:mcpClients:commands:userInputPromptHandler:setPlanModeActive:)` returning `[(any Tool, ToolMetadata)]`.
    - 36 entries, one per tool.

    For special tools:
    - AgentTool: isReadOnly: false, requiresApproval: true, interruptBehavior: .cancel, activityDescription: "Spawning sub-agent"
    - MCP tools (MCPTool, DynamicMCPTool, McpAuthTool): requiresApproval: true
    - AskUserQuestionTool: isReadOnly: true (reads user input)
    - Plan mode tools (EnterPlanModeTool, ExitPlanModeV2Tool): isReadOnly: true (gates behavior, no mutation)
    - Task tools (TaskCreate, TaskUpdate, TaskStop, TaskOutput): requiresApproval: false
    - Cron tools (CronCreate, CronDelete, CronList): isReadOnly: false (schedule mutation)
    - OverflowTestTool: isReadOnly: true (testing tool)

    For all other tools, derive metadata from old file overrides (searchHint, isEnabled, isReadOnly, isConcurrencySafe, isDestructive, interruptBehavior, activityDescription, requiresApproval).

    This is the final registry. Combined with Batch1ToolRegistry (15 tools, 04-02) and Batch23ToolRegistry (9 tools, 04-03), all 63 tools are covered.
  </action>
  <acceptance_criteria>
    - Entries: `grep -c 'ToolMetadata(' Sources/SwiftAgentCore/AgentRuntime/Tools/Batch45ToolRegistry.swift` returns 36
    - AgentTool entry: `grep -c 'AgentTool(' Sources/SwiftAgentCore/AgentRuntime/Tools/Batch45ToolRegistry.swift` returns at least 1
    - MCPTool entry: `grep -c 'MCPTool(' Sources/SwiftAgentCore/AgentRuntime/Tools/Batch45ToolRegistry.swift` returns at least 1
    - AskUserQuestionTool entry: `grep -c 'AskUserQuestionTool(' Sources/SwiftAgentCore/AgentRuntime/Tools/Batch45ToolRegistry.swift` returns at least 1
    - Return type uses `any Tool`: `grep -c 'any Tool' Sources/SwiftAgentCore/AgentRuntime/Tools/Batch45ToolRegistry.swift` returns at least 1
    - Build: `swift build --disable-sandbox --target SwiftAgentCore` compiles with zero errors
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'ToolMetadata(' Sources/SwiftAgentCore/AgentRuntime/Tools/Batch45ToolRegistry.swift</automated>
  </verify>
  <done>Batch45ToolRegistry created with metadata for all 36 Batch 4+5 tools. All 63 tools now have registry entries across 3 registries. MIG-01 complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AgentTool sub-agent | Child LanguageModelSessionImpl inherits parent providers |
| AskUserQuestionTool | User input crosses tool boundary via closure |
| MCP client boundary | External MCP server processes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Elevation | AgentTool child session | mitigate | Child session uses same PermissionEngine as parent; permission escalation blocked |
| T-04-09 | Info Disclosure | AskUserQuestionTool | mitigate | User input displayed locally; no exfiltration |
| T-04-10 | Spoofing | MCPTool server | accept | MCP identity verified at transport layer (not this phase) |
| T-04-11 | DoS | CronCreateTool | accept | Rate limiting is future concern; no DoS vector without malicious input |
| T-04-SC | Tampering | npm/pip/cargo | accept | No external packages installed |
</threat_model>

<verification>
1. `ls Sources/SwiftAgentCore/Tools/*.swift` — zero results (directory empty)
2. All 63 tools in AgentRuntime/Tools/ conform to Tool (not RuntimeAgentTool)
3. RuntimeAgentTool typealias exists for backward compatibility
4. 3 shared types in Types/ (BuiltInAgents, CronTypes, WorktreeTypes)
5. 3 batch registries cover all tools (Batch1, Batch23, Batch45)
6. Zero ToolUseContext references in AgentRuntime/Tools/
7. Core target compiles (tool errors resolved; consumer errors expected and addressed in subsequent plans)
</verification>

<success_criteria>
Tool migration (MIG-01) complete. All 63 tools on Tool protocol. Old Tools/ directory empty. Shared types relocated. Protocol renamed to Tool. All 3 registries created. Unmigrated consumers (ChatCommand, ThreadViewModel) still have compile errors — these are addressed in Plans 04-05 and 04-06.
</success_criteria>

<output>
Create `.planning/phases/04-migration-wiring-cleanup/04-04-SUMMARY.md` when done.
</output>

## Artifacts This Phase Produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| `Tool` protocol | protocol (renamed) | `AgentRuntime/Tools/RuntimeAgentTool.swift` | Renamed from RuntimeAgentTool after old Tool protocol deletion in 04-03 |
| `RuntimeAgentTool` typealias | typealias | `AgentRuntime/Tools/RuntimeAgentTool.swift` | Backward compat alias — `public typealias RuntimeAgentTool = Tool` |
| 20 Batch 4 tools | struct: Tool | `AgentRuntime/Tools/*.swift` | Task/agent/workflow tools created in Task 1 |
| 16 Batch 5 tools | struct: Tool | `AgentRuntime/Tools/*.swift` | MCP/config/cron/interactive tools created in Task 2 |
| `AgentTool` | struct: Tool | `AgentRuntime/Tools/AgentTool.swift` | Sub-agent spawning via LanguageModelSessionImpl |
| `Batch45ToolRegistry.tools(...)` | static func | `AgentRuntime/Tools/Batch45ToolRegistry.swift` | 36 tool registry created in Task 3 |
| `BuiltInAgents` | file (relocated) | `Types/BuiltInAgents.swift` | Moved from Tools/ |
| `CronTypes` | file (relocated) | `Types/CronTypes.swift` | Moved from Tools/ |
| `WorktreeTypes` | file (relocated) | `Types/WorktreeTypes.swift` | Moved from Tools/ |
| 36 old tool files deleted | deletion | `Sources/SwiftAgentCore/Tools/` | Final batch removed |
