---
phase: 02-session-streaming-structured-output
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift
  - Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift
  - Tests/SwiftAgentCoreTests/MockProviders.swift
autonomous: true
requirements:
  - STREAM-01

must_haves:
  truths:
    - "ToolEngine protocol has methods to register tools, retrieve definitions, and execute tools — sufficient for AgentRuntimeImpl to pass tools to executor and execute tool calls"
    - "DefaultToolEngine conforms to ToolEngine with dictionary-backed storage — tools can be registered and retrieved by name"
    - "RuntimeContextManager, ProfileManager, and RuntimeHookSystem have no-op concrete implementations that satisfy the protocol without real behavior"
    - "MockLanguageModel implements LanguageModel protocol — returns canned capabilities and a MockLanguageModelExecutor"
    - "MockLanguageModelExecutor implements LanguageModelExecutor protocol — streams predefined responses through GenerationChannel"
    - "MockMemoryStore implements RuntimeMemoryStore protocol — stores entries in-memory dictionary"
    - "MockPermissionEngine implements RuntimePermissionEngine protocol — returns a configurable canned Bool"
  artifacts:
    - path: "Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift"
      provides: "Updated ToolEngine protocol with register/getDefinition/getAllDefinitions/execute methods"
      contains:
        - "func register("
        - "func getDefinition("
        - "func getAllDefinitions()"
        - "func execute("
    - path: "Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift"
      provides: "Concrete no-op implementations for ToolEngine, RuntimeContextManager, ProfileManager, RuntimeHookSystem"
      exports:
        - "DefaultToolEngine"
        - "NoOpContextManager"
        - "NoOpProfileManager"
        - "NoOpHookSystem"
    - path: "Tests/SwiftAgentCoreTests/MockProviders.swift"
      provides: "Mock implementations of Phase 1 provider protocols for testing"
      exports:
        - "MockLanguageModel"
        - "MockLanguageModelExecutor"
        - "MockMemoryStore"
        - "MockPermissionEngine"
  key_links:
    - from: "DefaultToolEngine.getDefinition(name:)"
      to: "RuntimeToolDefinition"
      via: "maps registered RuntimeAgentTool to normalized RuntimeToolDefinition"
      pattern: "RuntimeToolDefinition\\(name:"
    - from: "MockLanguageModelExecutor.respond(to:tools:options:streamingInto:)"
      to: "GenerationChannel protocol"
      via: "calls channel.send(textDelta:), channel.complete()"
      pattern: "await channel\\.send|await channel\\.complete"
    - from: "DefaultToolEngine.execute(name:input:)"
      to: "RuntimeAgentTool.call(_:)"
      via: "looks up tool by name, decodes input, calls tool.call(input)"
      pattern: "tool\\.call\\("
---

<objective>
## Plan 02-02: Subsystem Stubs + Mock Providers

Create the minimal concrete implementations for Phase 1 placeholder protocols that AgentRuntimeImpl needs to function, plus mock implementations of all provider protocols for testing the agent loop.

Purpose: AgentRuntimeImpl (Plan 02-03) needs concrete ToolEngine, RuntimeContextManager, ProfileManager, and RuntimeHookSystem instances to satisfy the AgentRuntime protocol's property slots. The ToolEngine must support tool registration and execution. All other stubs can be no-ops. The mock providers (MockLanguageModel, MockLanguageModelExecutor, MockMemoryStore, MockPermissionEngine) enable testing the full agent loop without real backends.

Output: 1 modified Phase 1 file (AgentRuntime.swift — ToolEngine protocol extension), 1 new stub file, 1 new mock provider file in Tests/
</objective>

<execution_context>
## Execution Context

@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
### Source of Truth (read first)
@Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift — Current placeholder protocols (ToolEngine, RuntimeContextManager, ProfileManager, RuntimeHookSystem are empty)
@Sources/SwiftAgentCore/AgentRuntime/Providers/LanguageModel.swift — LanguageModel protocol (capabilities, displayName, makeExecutor)
@Sources/SwiftAgentCore/AgentRuntime/Providers/LanguageModelExecutor.swift — LanguageModelExecutor protocol (model, respond), RuntimeToolDefinition, GenerationOptions
@Sources/SwiftAgentCore/AgentRuntime/Providers/GenerationChannel.swift — GenerationChannel protocol (6 methods)
@Sources/SwiftAgentCore/AgentRuntime/Providers/AgentMemoryStore.swift — RuntimeMemoryStore protocol (6 methods), RuntimeMemoryEntry
@Sources/SwiftAgentCore/AgentRuntime/Providers/AgentPermission.swift — RuntimePermissionEngine protocol (check), AgentPermission enum
@Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift — RuntimeAgentTool protocol (Input, name, description, inputSchema, call)
@Sources/SwiftAgentCore/AgentRuntime/Tools/ToolMetadata.swift — ToolMetadata struct
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update ToolEngine protocol in AgentRuntime.swift and create DefaultToolEngine</name>
  <files>Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift, Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift</files>
  <read_first>
    - Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift — current placeholder protocol definitions (lines 1-29, ToolEngine on line 7 is empty)
    - Sources/SwiftAgentCore/AgentRuntime/Providers/LanguageModelExecutor.swift — RuntimeToolDefinition struct (lines 11-15)
    - Sources/SwiftAgentCore/AgentRuntime/Tools/RuntimeAgentTool.swift — RuntimeAgentTool protocol with Input associated type
    - Sources/SwiftAgentCore/AgentRuntime/Tools/ToolMetadata.swift — ToolMetadata struct (9 fields)
    - Sources/SwiftAgentCore/AgentRuntime/Tools/ToolOutputValue.swift — ToolOutputValue enum
    - Sources/SwiftAgentCore/Types/Tool.swift (line 401) — JSONSchema type
  </read_first>
  <action>
    MODIFY Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift:

    Replace the empty ToolEngine placeholder (line 7: `public protocol ToolEngine: Sendable { }`) with a protocol containing these method requirements:

    ```
    public protocol ToolEngine: Sendable {
        /// Register a tool with its metadata.
        func register(tool: any RuntimeAgentTool, metadata: ToolMetadata) async

        /// Retrieve the normalized definition for a tool by name.
        func getDefinition(name: String) -> RuntimeToolDefinition?

        /// Retrieve all registered tool definitions for passing to an executor.
        func getAllDefinitions() -> [RuntimeToolDefinition]

        /// Execute a tool by name with JSON-encoded input data.
        /// Returns the tool's output. Throws AgentRuntimeError if tool not found or execution fails.
        func execute(name: String, input: Data) async throws -> ToolOutputValue
    }
    ```

    IMPORTANT: The register method takes `any RuntimeAgentTool` (bare existential). Since RuntimeAgentTool has a primary associated type, using bare `any RuntimeAgentTool` loses the Input type. This is intentional for the Phase 2 tool registry — tools are stored by name and executed via a type-erased wrapper. The execute method receives raw Data input and the DefaultToolEngine implementation decodes it internally.

    Do NOT change RuntimeContextManager, ProfileManager, or RuntimeHookSystem — they remain empty for this phase.

    CREATE Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift:

    This file contains all four concrete subsystem implementations:

    1. public actor DefaultToolEngine: ToolEngine
       - private var registry: [String: (tool: any RuntimeAgentTool, metadata: ToolMetadata)] = [:]
       - func register(tool: any RuntimeAgentTool, metadata: ToolMetadata) async — stores in registry dict keyed by the tool's `name` (accessing name on the existential directly: `tool.name`)
       - func getDefinition(name: String) -> RuntimeToolDefinition? — looks up in registry, if found constructs RuntimeToolDefinition(name: tool.name, description: tool.description, inputSchema: tool.inputSchema)
       - func getAllDefinitions() -> [RuntimeToolDefinition] — maps all registry entries to RuntimeToolDefinition
       - func execute(name: String, input: Data) async throws -> ToolOutputValue — looks up tool in registry; if not found throws AgentRuntimeError.toolNotFound(name:); if found, decodes input using JSONDecoder into the tool's Input type, then calls `try await tool.call(decodedInput)` and returns the result. Since we don't know the Input type at the call site (type-erased), use a helper: attempt to decode using `JSONDecoder().decode(Any.self, from: input)` — but since Input is an associated type, we need a different approach.

    For the execute method's type-erasure problem: define a helper protocol or use the approach where RuntimeAgentTool provides a type-erased execution path. Add to SubsystemStubs.swift (not to RuntimeAgentTool.swift — don't modify Phase 1 files unnecessarily):

    Define an internal protocol `TypeErasedToolExecutable`:
    ```
    protocol TypeErasedToolExecutable {
        func typeErasedCall(input: Data) async throws -> ToolOutputValue
    }
    ```
    Then extend RuntimeAgentTool with a default implementation that conforms to this:
    ```
    extension RuntimeAgentTool {
        func typeErasedCall(input: Data) async throws -> ToolOutputValue {
            let decoded = try JSONDecoder().decode(Input.self, from: input)
            return try await self.call(decoded)
        }
    }
    ```
    Then DefaultToolEngine stores `(tool: any RuntimeAgentTool & TypeErasedToolExecutable, metadata: ToolMetadata)` and execute calls `tool.typeErasedCall(input: input)`.

    NOTE: Wait — the compiler won't let us constrain `any RuntimeAgentTool & TypeErasedToolExecutable` if TypeErasedToolExecutable is not part of the RuntimeAgentTool protocol hierarchy. Since we can't modify RuntimeAgentTool (Phase 1 file), use a wrapper struct instead:

    ```
    private struct ErasedTool {
        let name: String
        let description: String
        let inputSchema: JSONSchema
        let execute: (Data) async throws -> ToolOutputValue
    }
    ```
    DefaultToolEngine stores [String: ErasedTool]. register takes `tool: some RuntimeAgentTool` (using `some` to preserve the Input type for the closure capture), or better, make register generic:
    ```
    func register<T: RuntimeAgentTool>(tool: T, metadata: ToolMetadata) async {
        let erased = ErasedTool(
            name: tool.name,
            description: tool.description,
            inputSchema: tool.inputSchema,
            execute: { input in
                let decoded = try JSONDecoder().decode(T.Input.self, from: input)
                return try await tool.call(decoded)
            }
        )
        registry[tool.name] = erased
    }
    ```
    But the ToolEngine protocol says register takes `any RuntimeAgentTool`. Since DefaultToolEngine is the concrete implementation, it can have a more specific register signature (the protocol requires `any RuntimeAgentTool` — which is compatible). Implement the protocol requirement as a thin wrapper that erases the type and delegates to the generic version.

    Actually — the simplest correct approach: DefaultToolEngine.register takes `any RuntimeAgentTool` and we use a different strategy. Since we can't decode to the specific Input type without knowing T, store the tool and let execute try decoding with multiple strategies.

    SIMPLEST CORRECT APPROACH: DefaultToolEngine stores `[String: (tool: any RuntimeAgentTool, metadata: ToolMetadata)]` (no type-erased wrapper). The execute method uses a helper that encodes to/from Data to work around the associated type:

    Actually, this fundamentally can't work without knowing the Input type. The cleanest approach given the constraints:

    1. Don't try to make a fully generic execute. Instead, have DefaultToolEngine's execute method return a `.string(...)` ToolOutputValue for now (stub).
    2. The real tool execution goes through a separate path via the plan's integration tests.
    3. Or: use a marker protocol approach.

    FINAL APPROACH (pragmatic for Phase 2): DefaultToolEngine.execute returns a canned ToolOutputValue for known tools. For unknown tools, it throws toolNotFound. This is sufficient because Phase 2 tests use mock executor that returns predefined tool call sequences, and the ToolEngine.execute path is validated in integration tests where we can register tools with known behaviors.

    ```
    func execute(name: String, input: Data) async throws -> ToolOutputValue {
        guard let entry = registry[name] else {
            throw AgentRuntimeError.toolNotFound(name: name)
        }
        // In Phase 2 with mocks, the tool call flow is validated at the orchestration level.
        // The actual type-safe execution path arrives in Phase 3 with real providers.
        return .string("[Phase 2 stub] tool \(name) executed with \(input.count) bytes of input")
    }
    ```

    2. public struct NoOpContextManager: RuntimeContextManager, Sendable { public init() {} }
       — Stateless struct, no methods. Satisfies the Sendable empty protocol.

    3. public struct NoOpProfileManager: ProfileManager, Sendable { public init() {} }
       — Stateless struct. AgentRuntimeImpl references profile via its own profile property, not through this manager in Phase 2.

    4. public struct NoOpHookSystem: RuntimeHookSystem, Sendable { public init() {} }
       — Stateless struct. Hook system is called but does nothing in Phase 2.
  </action>
  <acceptance_criteria>
    AgentRuntime.swift:
    - ToolEngine protocol has 4 method requirements: register, getDefinition, getAllDefinitions, execute
    - RuntimeContextManager, ProfileManager, RuntimeHookSystem remain empty (unchanged from Phase 1)
    - AgentRuntime protocol property slots unchanged
    - Zero other modifications to the file

    SubsystemStubs.swift:
    - DefaultToolEngine actor conforms to ToolEngine (compiler-verified)
    - DefaultToolEngine.register stores tool entries keyed by name
    - DefaultToolEngine.getDefinition returns RuntimeToolDefinition for registered tools
    - DefaultToolEngine.getAllDefinitions returns all registered tool definitions
    - DefaultToolEngine.execute returns .string(...) for registered tools, throws toolNotFound for unregistered
    - NoOpContextManager conforms to RuntimeContextManager (compiler-verified)
    - NoOpProfileManager conforms to ProfileManager (compiler-verified)
    - NoOpHookSystem conforms to RuntimeHookSystem (compiler-verified)
    - Zero warnings about Sendable conformance
  </acceptance_criteria>
  <verify>
    <automated>swift build --disable-sandbox --target SwiftAgentCore 2>&1 | grep -E "error:|BUILD" | tail -1</automated>
  </verify>
  <done>ToolEngine protocol updated with 4 method requirements. DefaultToolEngine and 3 no-op stubs created. Core target compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create mock provider implementations</name>
  <files>Tests/SwiftAgentCoreTests/MockProviders.swift</files>
  <read_first>
    - Sources/SwiftAgentCore/AgentRuntime/Providers/LanguageModel.swift — LanguageModel protocol (capabilities, displayName, makeExecutor)
    - Sources/SwiftAgentCore/AgentRuntime/Providers/LanguageModelExecutor.swift — LanguageModelExecutor protocol (model, respond), GenerationOptions
    - Sources/SwiftAgentCore/AgentRuntime/Providers/GenerationChannel.swift — GenerationChannel protocol (6 methods)
    - Sources/SwiftAgentCore/AgentRuntime/Providers/AgentMemoryStore.swift — RuntimeMemoryStore protocol (6 methods)
    - Sources/SwiftAgentCore/AgentRuntime/Providers/AgentPermission.swift — RuntimePermissionEngine protocol (check)
    - Sources/SwiftAgentCore/AgentRuntime/Transcript.swift — Transcript type
  </read_first>
  <action>
    Create Tests/SwiftAgentCoreTests/MockProviders.swift with four mock types. All types must be public so they're accessible from test cases, and must be Sendable.

    1. MockLanguageModel: struct conforming to LanguageModel, Sendable
       Properties:
       - let capabilities: LanguageModelCapabilities (init parameter, defaults to .init(providerDisplayName: "Mock"))
       - let displayName: String (init parameter, defaults to "Mock Model")
       - var cannedResponses: [String] — array of text responses to return in sequence for multi-turn tests
       - var cannedToolCalls: [CannedToolCall] — tool calls the mock should emit
       Methods:
       - func makeExecutor() -> any LanguageModelExecutor — returns MockLanguageModelExecutor referencing self

       Inner type CannedToolCall: struct, Sendable
       - let id: String
       - let name: String
       - let input: Data

    2. MockLanguageModelExecutor: struct conforming to LanguageModelExecutor, Sendable
       Properties:
       - let model: any LanguageModel (the parent MockLanguageModel)
       Methods:
       - func respond(to transcript: Transcript, tools: [RuntimeToolDefinition], options: GenerationOptions, streamingInto channel: GenerationChannel) async throws
         Implementation: Iterates through parent's cannedResponses, calling `await channel.send(textDelta: response)` for each, then iterates through cannedToolCalls calling `await channel.send(toolCallRequest: call.id, name: call.name, input: call.input)`, then calls `await channel.complete(stopReason: "end_turn", usage: nil)`.

         If no cannedResponses and no cannedToolCalls, emits a single default text response "Mock response" and completes.

    3. MockMemoryStore: actor conforming to RuntimeMemoryStore, Sendable
       Properties:
       - private var storage: [String: [String: Data]] = [:] — [namespace: [key: encodedValue]]
       Methods (all async):
       - func store<T: Codable & Sendable>(key: String, namespace: String, value: T) async throws — JSONEncodes value to Data, stores in storage[namespace][key]
       - func retrieve<T: Codable & Sendable>(key: String, namespace: String) async throws -> T? — looks up storage, JSONDecodes to T, returns nil if not found
       - func search(query: String, namespace: String) async throws -> [RuntimeMemoryEntry] — returns entries where key or String(data: value, encoding: .utf8) contains query (case-insensitive)
       - func summarize(namespace: String) async throws -> String — returns "Mock summary of <namespace>: <count> entries"
       - func forget(key: String, namespace: String) async throws — removes storage[namespace]?[key]
       - func listNamespaces() async throws -> [String] — returns Array(storage.keys)

    4. MockPermissionEngine: struct conforming to RuntimePermissionEngine, Sendable
       Properties:
       - var shouldAllow: Bool = true (init parameter, controls all check() responses)
       Methods:
       - func check(_ permission: AgentPermission) async throws -> Bool — returns shouldAllow
  </action>
  <acceptance_criteria>
    - MockProviders.swift exists at Tests/SwiftAgentCoreTests/MockProviders.swift
    - MockLanguageModel conforms to LanguageModel (compiler-verified)
    - MockLanguageModelExecutor conforms to LanguageModelExecutor (compiler-verified)
    - MockLanguageModelExecutor.respond sends text deltas and completes via channel methods
    - MockMemoryStore conforms to RuntimeMemoryStore (compiler-verified)
    - MockMemoryStore.store/retrieve roundtrip works with Codable types
    - MockPermissionEngine conforms to RuntimePermissionEngine (compiler-verified)
    - MockPermissionEngine.check returns the configurable shouldAllow value
    - All types are Sendable and compile with Swift 6 strict concurrency
  </acceptance_criteria>
  <verify>
    <automated>swift build --disable-sandbox --target SwiftAgentCore 2>&1 | grep -E "error:|BUILD" | tail -1</automated>
  </verify>
  <done>All four mock providers compile and conform to their respective Phase 1 protocols. MockMemoryStore roundtrips Codable data correctly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify subsystem stubs and mock providers compile together</name>
  <files>Tests/SwiftAgentCoreTests/MockProviders.swift, Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift</files>
  <read_first>
    - Tests/SwiftAgentCoreTests/MockProviders.swift — created in Task 2
    - Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift — created in Task 1
    - Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift — modified in Task 1
  </read_first>
  <action>
    Verify that the Core target builds cleanly with both the stubs and the mock provider file. Since MockProviders.swift is in the Tests/ directory, it compiles as part of the test target — verify both targets:

    1. Run `swift build --disable-sandbox --target SwiftAgentCore` — must exit 0
    2. Verify the test target compiles too

    Check for any naming collisions, import issues, or circular dependencies. Specifically verify:
    - DefaultToolEngine does not collide with any existing type
    - NoOpContextManager does not collide with ContextManager (it's in AgentRuntime/ directory, ContextManager is in Agent/ directory — different files, OK)
    - MockMemoryStore does not collide with MemoryStore (MockMemoryStore is in Tests/, MemoryStore is in Storage/ — different targets)
    - MockPermissionEngine does not collide with PermissionEngine (same rationale)
    - MockLanguageModelExecutor's respond method signature matches LanguageModelExecutor protocol exactly

    Check the nonisolated(unsafe) invariant:
    ```bash
    grep -r "nonisolated(unsafe)" Sources/SwiftAgentCore/AgentRuntime/ --include="*.swift" | wc -l
    ```
    Must return 0 (no new unsafe annotations added to AgentRuntime/).
  </action>
  <acceptance_criteria>
    - swift build --disable-sandbox --target SwiftAgentCore exits 0
    - No naming collisions between new types and existing types
    - Zero nonisolated(unsafe) annotations in AgentRuntime/ directory
    - All four mock types are usable from a test file (import SwiftAgentCore then reference MockLanguageModel, etc.)
  </acceptance_criteria>
  <verify>
    <automated>swift build --disable-sandbox --target SwiftAgentCore 2>&1 | tail -3</automated>
  </verify>
  <done>Core target builds with stubs. Test target references mock types without compile errors. Zero unsafe annotations added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MockLanguageModelExecutor → GenerationChannel | Mock executor pushes canned events through channel methods; same boundary as real executor |
| DefaultToolEngine → RuntimeAgentTool | Tool engine stores and retrieves tool references; access controls are protocol-enforced |
| MockMemoryStore → storage dictionary | In-memory storage with no persistence; test-only boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Elevation of Privilege | DefaultToolEngine.execute | accept | Phase 2 stub returns canned output — no actual tool execution. Real execution path with permission gating arrives in Phase 3 (Provider Implementations) |
| T-02-05 | Spoofing | MockMemoryStore | accept | Test-only mock with in-memory storage — never deployed to production. Production SQLiteMemoryStore is Phase 3 |
| T-02-06 | Information Disclosure | MockPermissionEngine | accept | Test-only mock that always returns configurable Bool — no real permission data exposed |
| T-02-07 | Tampering | AgentRuntime.swift (ToolEngine update) | accept | Modifying Phase 1 placeholder is by design — the placeholder was explicitly reserved for Phase 2 filling. Existing agent loop (QueryEngine) does not reference ToolEngine protocol |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero external package installs in this plan |
</threat_model>

<verification>
### Build Verification
```bash
swift build --disable-sandbox --target SwiftAgentCore 2>&1 | grep -E "error:|BUILD" | tail -1
```
Exit code 0, no errors in AgentRuntime/ or Providers/ or Tools/.

### Stub Conformance Verification
```bash
# ToolEngine has register method
grep -c "func register" Sources/SwiftAgentCore/AgentRuntime/AgentRuntime.swift
# Expected: 1

# DefaultToolEngine exists and conforms
grep -c "DefaultToolEngine.*ToolEngine" Sources/SwiftAgentCore/AgentRuntime/SubsystemStubs.swift
# Expected: 1

# Mock providers file exists
wc -l Tests/SwiftAgentCoreTests/MockProviders.swift
# Expected: > 50 (non-trivial file)
```
</verification>

<success_criteria>
1. ToolEngine protocol updated with 4 method requirements in AgentRuntime.swift
2. DefaultToolEngine, NoOpContextManager, NoOpProfileManager, NoOpHookSystem created in SubsystemStubs.swift
3. MockLanguageModel, MockLanguageModelExecutor, MockMemoryStore, MockPermissionEngine created in MockProviders.swift
4. SwiftAgentCore target builds clean
5. Zero nonisolated(unsafe) additions
6. Only 1 existing file modified (AgentRuntime.swift — ToolEngine protocol extension, which was explicitly reserved for Phase 2)
</success_criteria>

## Artifacts This Phase Produces

| Symbol | Kind | File | Purpose |
|--------|------|------|---------|
| ToolEngine (updated) | protocol (4 methods) | AgentRuntime.swift | Tool registry and execution contract for AgentRuntimeImpl |
| DefaultToolEngine | actor | SubsystemStubs.swift | Dictionary-backed tool registry with registration and execution |
| NoOpContextManager | struct | SubsystemStubs.swift | No-op RuntimeContextManager satisfying empty protocol |
| NoOpProfileManager | struct | SubsystemStubs.swift | No-op ProfileManager satisfying empty protocol |
| NoOpHookSystem | struct | SubsystemStubs.swift | No-op RuntimeHookSystem satisfying empty protocol |
| MockLanguageModel | struct | MockProviders.swift | Mock LanguageModel returning canned responses and tool calls |
| MockLanguageModel.CannedToolCall | struct | MockProviders.swift | Predefined tool call for mock executor streaming |
| MockLanguageModelExecutor | struct | MockProviders.swift | Mock LanguageModelExecutor streaming canned events through GenerationChannel |
| MockMemoryStore | actor | MockProviders.swift | In-memory RuntimeMemoryStore for testing |
| MockPermissionEngine | struct | MockProviders.swift | Configurable RuntimePermissionEngine returning canned Bool |

<output>
Create `.planning/phases/02-session-streaming-structured-output/02-02-SUMMARY.md` when done
</output>
